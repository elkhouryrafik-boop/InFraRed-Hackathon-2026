# CoolSpend — Hugging Face Spaces (Gradio SDK) dependency set.
# infrared-sdk is only required for the live Infrared backend (INFRARED_BACKEND=live);
# it is lazily imported and absence does not break offline/mock operation.
pymoo==0.6.1
gradio==4.44.1
gradio-client==1.3.0  # MUST match gradio 4.44.1 — a mismatched client causes "bool is not iterable" at launch
fastapi==0.112.4      # gradio 4.44.1-compatible; newer fastapi pulls starlette>=1.0 which breaks TemplateResponse
starlette==0.37.2     # starlette>=1.0 removes the TemplateResponse(name, context) signature gradio 4.44.1 calls -> "unhashable type: dict"
pyproj>=3.6,<4
shapely>=2.0,<3.0
numpy>=1.26,<2.0
matplotlib>=3.7,<4.0
geojson>=3.0,<4.0
infrared-sdk
requests>=2.31          # Open Data BCN inventory fetch (bcn_data) + ICGC WMS/raster
rasterio>=1.3           # OPTIONAL — ICGC LiDAR canopy-height sampling (bcn_lidar). Absent -> canopy context degrades to None, pipeline unaffected.
huggingface_hub==0.36.2
typer==0.12.5         # typer>=0.13 uses click.Choice[T] subscript unsupported in click 8.1.7 (gradio 4.44.1 dep) -> TypeError at import
