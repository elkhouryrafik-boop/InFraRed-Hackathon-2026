---
phase: 02-optimizer-core
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - coolspend/sdk_client.py
  - coolspend/tests/test_sdk_client_live.py
  - MOCKS.md
autonomous: true
requirements: [OPT-03]
tags: [sdk, live, infrared, utci, real-call, offline-default]
user_setup:
  - service: infrared.city
    why: "Real UTCI before/after validation of the Top-3 tree configs (OPT-03 live path)"
    env_vars:
      - name: INFRARED_API_KEY
        source: "infrared.city hackathon get-key page (issued at kickoff May 27 17:00 CET)"
    dashboard_config: []

must_haves:
  truths:
    - "With INFRARED_BACKEND=mock (default, no key) the full client still runs offline exactly as before"
    - "The live backend path is real code that calls the infrared_sdk (InfraredClient.run_area_and_wait), not a NotImplementedError stub"
    - "INFRARED_BACKEND=live without INFRARED_API_KEY raises EnvironmentError; the key is read from env only and never logged"
    - "A live result is written to the cached backend so a subsequent INFRARED_BACKEND=cached run replays it offline"
  artifacts:
    - path: "coolspend/sdk_client.py"
      provides: "live backend wired to infrared_sdk; lazy guarded import; live→cache write; UTCIResult unchanged"
      contains: "run_area_and_wait"
    - path: "coolspend/tests/test_sdk_client_live.py"
      provides: "offline tests: live without key raises; live import is lazy; mock default unchanged; live writes cache (monkeypatched SDK)"
      contains: "def test_"
  key_links:
    - from: "coolspend/sdk_client.py::_live_utci"
      to: "infrared_sdk.InfraredClient"
      via: "lazy import inside the live branch only"
      pattern: "from infrared_sdk import"
    - from: "coolspend/sdk_client.py::_dispatch live branch"
      to: "CACHE_DIR/{metric}_{hash}.json"
      via: "write live result to cache for cached-backend replay"
      pattern: "cache_file.write_text"
---

<objective>
Replace the `NotImplementedError` live stub in `coolspend/sdk_client.py` with a REAL working
Infrared SDK call (OPT-03 live path), without breaking the offline mock default.

When `INFRARED_BACKEND=live` and `INFRARED_API_KEY` is set, the client calls the genuine
`infrared_sdk` (ported from `infrared_client_v2.py` intent + the brief's quickstart:
`InfraredClient`, `client.buildings.get_area`, `run_area_and_wait`, `AnalysesName` UTCI/thermal,
`result.merged_grid` → scalar UTCI → `UTCIResult`). The live result is cached to disk so a later
`INFRARED_BACKEND=cached` run replays it offline. Mock stays the default; no key required to test.

The USER will plug `INFRARED_API_KEY` at kickoff (May 27). This plan makes live a working code
path, NOT a stub — verified offline by monkeypatching the SDK so no real key/network is needed now.

Purpose: this is the live half of the surrogate-optimize → validate-Top-3 pattern. The optimizer
(Plan 02-04) calls `get_baseline_utci` / `get_intervention_utci` for the 3 Top-3 configs only,
guarded by SimBudget.

Output: rewired `coolspend/sdk_client.py`, `coolspend/tests/test_sdk_client_live.py`, MOCKS.md note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/01-foundation-sdk-boundary/01-01-SUMMARY.md
@coolspend/sdk_client.py
@infrared_hackathon.md

<interfaces>
<!-- Existing sdk_client.py contract (Phase 1) — DO NOT change UTCIResult fields or
     the public function signatures. Only replace the live branch in _dispatch. -->

Existing public API (keep stable):
```python
@dataclass
class UTCIResult:
    utci_c: float; metric: str; backend: str; geometry_hash: str
    disclaimer: str; source: str
def get_baseline_utci(geometry: dict) -> UTCIResult
def get_intervention_utci(geometry: dict) -> UTCIResult
class SimBudget:  # record(label) logs + raises past cap
```

Real SDK quickstart (from infrared_hackathon.md — the live call pattern to port):
```python
from infrared_sdk import InfraredClient
from infrared_sdk.analyses.types import AnalysesName  # UTCI / thermal comfort
polygon = {"type": "Polygon", "coordinates": [[[lon,lat], ...]]}
with InfraredClient() as client:                 # reads INFRARED_API_KEY from env
    area = client.buildings.get_area(polygon)
    result = client.run_area_and_wait(<UTCIRequest analysis_type=AnalysesName.<utci>>,
                                      polygon, buildings=area.buildings)
print(result.merged_grid)  # numpy array
```
Note: exact UTCI request class / AnalysesName member name is confirmed against the installed
infrared_sdk at execution time (the SDK is installed after May 27). Until then the live branch is
exercised via a monkeypatched fake client in tests.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the live Infrared backend (lazy import, live→cache, key-from-env only)</name>
  <read_first>
    - WRITE: coolspend/sdk_client.py (replace ONLY the `if backend == "live"` branch in
      _dispatch and add a `_live_utci` helper; do not change UTCIResult, SimBudget, mock fns,
      or public signatures)
    - REFERENCE (port FROM): infrared_client_v2.py lines 331-348 (_dispatch shape, live/cache
      write pattern) and infrared_hackathon.md lines 181-194 (real InfraredClient quickstart).
      REFERENCE: CONCERNS.md 2.4 (coordinate footgun — convert via spatial_engine only),
      01-01-SUMMARY.md (existing contract).
  </read_first>
  <action>
    In `coolspend/sdk_client.py`:

    1. Add a `_live_utci(metric_key: str, geometry: dict) -> UTCIResult` helper:
       - Read `api_key = os.environ.get("INFRARED_API_KEY")`; if None raise `EnvironmentError`
         (keep existing message). NEVER log or include the key in any string.
       - LAZY import inside the function body only:
         `from infrared_sdk import InfraredClient` and
         `from infrared_sdk.analyses.types import AnalysesName` — wrap in try/except ImportError
         raising a clear RuntimeError ("infrared_sdk not installed — `pip install infrared-sdk`")
         so importing sdk_client.py never requires the SDK (CONCERNS 3.2 — offline before May 27).
       - Build the polygon: geometry must carry a "polygon_lonlat" list of (lon, lat) pairs OR
         be derivable from spatial_engine. To respect the single-CRS boundary (CONCERNS 2.4),
         convert any local-metre coords to lon/lat ONLY via
         `from coolspend.spatial_engine import local_m_to_latlon` — do NOT inline any projection.
       - `with InfraredClient() as client:` → `area = client.buildings.get_area(polygon)` →
         `result = client.run_area_and_wait(<UTCI request>, polygon, buildings=area.buildings)`.
         Reduce `result.merged_grid` to a scalar UTCI: `utci_c = float(np.mean(result.merged_grid))`
         (site-mean felt temperature). Use `import numpy as np` lazily inside the helper.
       - Return `UTCIResult(utci_c=round(utci_c,2), metric="utci_at_1.1m", backend="live",
         geometry_hash=_geometry_hash(geometry), disclaimer="LIVE Infrared SDK result",
         source="infrared.city SDK run_area_and_wait (UTCI)")`.
       - Add an inline TODO comment noting the exact UTCI request class / AnalysesName member is
         confirmed against the installed SDK at May-27 wiring time; keep a single named local
         (e.g. `utci_request = ...`) so the confirmation is a one-line change.

    2. Replace the live branch in `_dispatch`: keep the `EnvironmentError` key check, then call
       `result = _live_utci(metric_key, geometry)`, write it to `cache_file` via
       `cache_file.write_text(json.dumps(result.to_dict(), indent=2), encoding="utf-8")`
       (so INFRARED_BACKEND=cached replays it), and `return result`. Remove the NotImplementedError.

    3. Keep mock + cached branches byte-identical to Phase 1.

    Add a MOCKS.md note row: live UTCI result tagged "VERIFIED (live)" vs mock "NOT MEASURED" —
    clarify provenance distinction for the decision artifact.
  </action>
  <verify>
    <automated>python -m pytest coolspend/tests/test_sdk_client.py coolspend/tests/test_sdk_client_live.py -q</automated>
  </verify>
  <done>
    Importing coolspend.sdk_client with no infrared_sdk installed still succeeds (lazy import);
    INFRARED_BACKEND=mock default behavior unchanged (existing test_sdk_client.py still passes);
    INFRARED_BACKEND=live without key raises EnvironmentError; `grep -q "run_area_and_wait"
    coolspend/sdk_client.py` and `grep -q "from infrared_sdk import" coolspend/sdk_client.py`
    succeed; `grep -c "NotImplementedError" coolspend/sdk_client.py` returns 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Offline tests for the live path via monkeypatched fake SDK</name>
  <read_first>
    - WRITE: coolspend/tests/test_sdk_client_live.py (new)
    - REFERENCE: coolspend/tests/test_sdk_client.py (existing test conventions — pytest,
      monkeypatch env, offline)
  </read_first>
  <action>
    Create `coolspend/tests/test_sdk_client_live.py` (all offline — no real key, no network):
      - `test_import_does_not_require_sdk`: `import coolspend.sdk_client` succeeds even though
        infrared_sdk is absent (asserts the lazy-import design).
      - `test_live_without_key_raises`: monkeypatch INFRARED_BACKEND=live, delete INFRARED_API_KEY,
        assert `get_baseline_utci({...})` raises EnvironmentError.
      - `test_live_calls_sdk_and_caches`: monkeypatch INFRARED_BACKEND=live, set a dummy
        INFRARED_API_KEY, and inject a fake `infrared_sdk` module into sys.modules whose
        InfraredClient is a context manager returning a stub with `buildings.get_area` and
        `run_area_and_wait(...).merged_grid` = a small numpy array. Assert the returned
        UTCIResult.backend == "live", utci_c == mean of the fake grid, and that a cache file was
        written under CACHE_DIR (so a follow-up INFRARED_BACKEND=cached read returns the same value).
      - `test_key_never_logged`: capture logging output during a live call and assert the dummy
        key string does not appear in any log record.
    Clean up sys.modules / env in fixtures so other tests are unaffected.
  </action>
  <verify>
    <automated>python -m pytest coolspend/tests/test_sdk_client_live.py -q</automated>
  </verify>
  <done>
    All four tests pass offline; cached replay returns the same scalar as the live call;
    the dummy API key never appears in captured logs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → sdk_client | INFRARED_API_KEY secret crosses from environment into the live client |
| sdk_client → infrared.city API | Real network call billed against the hackathon quota |
| API response → cache file | Untrusted JSON written to disk and replayed by cached backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Information disclosure | INFRARED_API_KEY | mitigate | Read from os.environ only; never logged, never put in UTCIResult/source/cache; test_key_never_logged enforces |
| T-02-08 | Denial of service / cost | runaway live calls | mitigate | SimBudget caps live calls (SDK-03); live path reachable only via explicit validate (Plan 02-04), never the NSGA-II hot path |
| T-02-09 | Tampering | cached JSON replay | mitigate | json.loads only (no eval); cache keyed by deterministic geometry hash |
| T-02-10 | Spoofing | wrong polygon CRS to API | mitigate | lon/lat conversion only via spatial_engine.local_m_to_latlon (single CRS boundary, CONCERNS 2.4) |
</threat_model>

<verification>
- `python -m pytest coolspend/tests/test_sdk_client.py coolspend/tests/test_sdk_client_live.py -q` passes.
- `INFRARED_BACKEND=mock python -m coolspend.sdk_client` still prints offline mock UTCI.
- `grep -c "NotImplementedError" coolspend/sdk_client.py` == 0.
</verification>

<success_criteria>
OPT-03 live path is real working code (not a stub): live calls the infrared_sdk, reduces
merged_grid to a scalar UTCI, returns a labelled live UTCIResult, and caches it for offline
replay. Mock remains the default and the module imports without the SDK installed. The API key is
env-only and never logged. All paths verified offline via a monkeypatched fake SDK.
</success_criteria>

<output>
After completion, create `.planning/phases/02-optimizer-core/02-03-SUMMARY.md`.
</output>
