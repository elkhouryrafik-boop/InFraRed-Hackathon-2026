---
phase: 02-optimizer-core
plan: "02"
type: tdd
wave: 1
depends_on: []
files_modified:
  - coolspend/spatial_engine.py
  - coolspend/tests/test_surrogate.py
  - MOCKS.md
autonomous: true
requirements: [OPT-02]
tags: [surrogate, thermal, delta-tmrt, porosity-fix, offline]
user_setup: []

must_haves:
  truths:
    - "delta_tmrt_surrogate returns a finite °C reduction that increases with shade fraction and is capped at MAX_TMRT_REDUCTION"
    - "The porosity-squared bug cannot reappear: passing shade_fraction once and shade_fraction-squared gives materially different results, and the body does not re-apply porosity"
    - "A whole-config tree thermal estimate (thermal_relief) is computable offline with zero SDK calls"
  artifacts:
    - path: "coolspend/spatial_engine.py"
      provides: "delta_tmrt_surrogate (fixed, porosity applied once) + shade_efficiency + thermal_relief over a tree config"
      contains: "def delta_tmrt_surrogate"
    - path: "coolspend/tests/test_surrogate.py"
      provides: "regression test for double-porosity bug + monotonic shade + cap"
      contains: "def test_delta_tmrt_no_double_porosity"
  key_links:
    - from: "coolspend/spatial_engine.py::thermal_relief"
      to: "coolspend/spatial_engine.py::delta_tmrt_surrogate"
      via: "per-tree surrogate aggregation"
      pattern: "delta_tmrt_surrogate\\("
---

<objective>
Port the FIXED analytical thermal surrogate into `coolspend/spatial_engine.py` so the NSGA-II
hot path (Plan 02-03) computes thermal relief with ZERO live SDK calls per chromosome (OPT-02).

Two pieces:
1. `shade_efficiency()` + `delta_tmrt_surrogate()` ported from `nature_nsga2_coolstock.py`
   lines 89-155 — the porosity-squared bug fixed (porosity applied EXACTLY ONCE in the caller,
   never re-applied in the body), with a regression test that pins the fix (CONCERNS 4.2).
2. `thermal_relief(config)` — aggregate a tree-coordinate config's surrogate ΔTmrt into a
   single site-level relief value the optimizer maximizes.

Purpose: the surrogate is the hot-path fitness for objective 1 (thermal). The real Infrared UTCI
is reserved for Top-3 validation only (Plan 02-04). This module must never call the SDK.

Output: extended `coolspend/spatial_engine.py`, `coolspend/tests/test_surrogate.py`, MOCKS.md row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONCERNS.md
@coolspend/spatial_engine.py

<interfaces>
<!-- The surrogate is ported from the reference. Key reference signatures: -->

From nature_nsga2_coolstock.py (REFERENCE — port the FIXED body, do NOT import this file):
```python
def shade_efficiency(tilt_deg: float, height_m: float) -> float: ...
def delta_tmrt_surrogate(shade_fraction, porosity_pct, tilt_deg, height_m) -> float:
    MAX_TMRT_REDUCTION = 12.0   # UNSOURCED cap — keep value, flag in MOCKS + output
    effective_shade = shade_fraction          # porosity applied ONCE by caller
    eff = shade_efficiency(tilt_deg, height_m)
    return min(MAX_TMRT_REDUCTION * effective_shade * eff, MAX_TMRT_REDUCTION)
```

Existing spatial_engine.py constants to reuse (already defined — do not redefine):
SITE_WIDTH_M=60.0, SITE_DEPTH_M=42.0, HERITAGE_BUFFER_M=5.0.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Port fixed surrogate + thermal_relief into spatial_engine (RED then GREEN)</name>
  <read_first>
    - WRITE: coolspend/spatial_engine.py (append; do not touch the CRS / load_site / collision
      sections — append a new SURROGATE section), coolspend/tests/test_surrogate.py (new)
    - REFERENCE (port FROM): nature_nsga2_coolstock.py lines 89-155 (shade_efficiency,
      delta_tmrt_surrogate — port the post-fix body where porosity is applied ONCE) and
      lines 81-86 (PEAK_SUN_ALTITUDE_DEG=63.0, PEAK_SUN_AZIMUTH_DEG=215.0). REFERENCE:
      CONCERNS.md sections 1.1 (surrogate honesty), 4.2 (regression test spec).
  </read_first>
  <behavior>
    - delta_tmrt_surrogate(shade_fraction=0.85, porosity_pct=15.0, tilt_deg=0, height_m=3.75)
      returns a finite positive float <= MAX_TMRT_REDUCTION (12.0).
    - Monotonic: delta_tmrt_surrogate(0.90, ...) > delta_tmrt_surrogate(0.50, ...) (more shade
      → more cooling), same other args.
    - Cap: delta_tmrt_surrogate(1.0, 0.0, 30, 5.0) <= 12.0 (never exceeds the cap).
    - Double-porosity regression (CONCERNS 4.2): the body must NOT square shade_fraction —
      delta_tmrt_surrogate(0.85, 15, 0, 3.75) != delta_tmrt_surrogate(0.85**2, 15, 0, 3.75),
      proving porosity is applied once, not twice.
    - thermal_relief(config) for a config with N active trees returns a finite, non-negative °C
      site-relief float that increases with the number of validly-placed trees and is 0.0 for an
      empty config. Deterministic.
  </behavior>
  <action>
    Append a `# ── THERMAL SURROGATE (OPT-02) ──` section to `coolspend/spatial_engine.py`.

    Add constants (each `# SOURCE: ...` honest tag):
      PEAK_SUN_ALTITUDE_DEG: float = 63.0   # Barcelona July solar noon (analytical geometry)
      PEAK_SUN_AZIMUTH_DEG: float = 215.0   # SW afternoon peak
      MAX_TMRT_REDUCTION_C: float = 12.0    # UNSOURCED cap — see MOCKS.md / CONCERNS 1.1
      TREE_SHADE_FRACTION: float = 0.80     # mature street-tree canopy shade fraction (DECLARED)
      TREE_CANOPY_RADIUS_M: float = 3.0     # effective shaded radius per tree (DECLARED)

    Port (exact signatures, fixed body):
      `def shade_efficiency(tilt_deg: float, height_m: float) -> float:` — copy reference body
        (tilt_factor = 1.0 + 0.15*(tilt_deg/30.0); height_factor = 1.0 + 0.05*((height_m-2.5)/2.5)).
        Use math (not numpy) to keep the module numpy-light; document source.
      `def delta_tmrt_surrogate(shade_fraction: float, porosity_pct: float = 0.0,
        tilt_deg: float = 0.0, height_m: float = 3.0) -> float:` — FIXED body:
        `effective_shade = shade_fraction` (porosity NOT re-applied — cite CONCERNS 1.1 / 4.2 in
        an inline comment), `eff = shade_efficiency(tilt_deg, height_m)`,
        `return min(MAX_TMRT_REDUCTION_C * effective_shade * eff, MAX_TMRT_REDUCTION_C)`.
        Docstring: analytical proxy, NOT measured, ±4°C uncertainty, replace with Ladybug/Infrared.

    Add the tree-config aggregator (NEW — coolspend is trees, not a single canopy):
      `def thermal_relief(config: dict) -> float:`
        Active trees = [t for t in config.get("trees", []) if t.get("active", True)].
        Per-tree under-canopy ΔTmrt = delta_tmrt_surrogate(TREE_SHADE_FRACTION). Each tree shades
        area = pi * TREE_CANOPY_RADIUS_M**2. total_shaded = sum capped at SITE_WIDTH_M*SITE_DEPTH_M.
        site_coverage_fraction = min(total_shaded / (SITE_WIDTH_M*SITE_DEPTH_M), 0.90).
        Return round(per_tree_delta * site_coverage_fraction, 3) (site-averaged relief). 0 trees → 0.0.

    RED first: write `coolspend/tests/test_surrogate.py` with the behaviors above (it will fail
    to import the new symbols → RED). Commit `test(02-02): ...`. Implement → GREEN. Commit
    `feat(02-02): ...`.

    Append a MOCKS.md row: "delta_tmrt_surrogate (analytical thermal proxy, MAX_TMRT_REDUCTION=12°C
    unsourced cap, ±4°C, Garcia-Nevado 2020 surface-temp proxy not Tmrt@1.1m)" status MOCK/SURROGATE.
  </action>
  <verify>
    <automated>python -m pytest coolspend/tests/test_surrogate.py -q</automated>
  </verify>
  <done>
    test_surrogate.py passes including test_delta_tmrt_no_double_porosity; surrogate is monotonic
    in shade and capped at 12.0; thermal_relief is finite/non-negative and 0 for empty config;
    `grep -q "def delta_tmrt_surrogate" coolspend/spatial_engine.py` and
    `grep -q "def thermal_relief" coolspend/spatial_engine.py` succeed; existing
    `python -m pytest coolspend/tests/test_spatial_engine.py coolspend/tests/test_coordinate_frame.py -q`
    still passes (no regression in CRS / collision sections).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chromosome config → surrogate | Untrusted numeric tree coordinates / counts from NSGA-II |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | surrogate output presented as measured | mitigate | Docstring + MOCKS row label surrogate "NOT MEASURED", ±4°C; cap flagged unsourced |
| T-02-05 | Information disclosure | spatial_engine surrogate | accept | Pure math, no IO/network/secrets |
| T-02-06 | Repudiation | silent porosity regression | mitigate | test_delta_tmrt_no_double_porosity pins the fix; CONCERNS 4.2 satisfied |
</threat_model>

<verification>
- `python -m pytest coolspend/tests/test_surrogate.py -q` passes.
- Full suite `python -m pytest coolspend/tests -q` still green (no regression).
- `grep -q "CONCERNS 1.1" coolspend/spatial_engine.py` (surrogate honesty rationale present).
</verification>

<success_criteria>
OPT-02 surrogate available in spatial_engine: a fast, offline, deterministic ΔTmrt proxy with the
double-porosity bug fixed and regression-pinned, plus a tree-config thermal_relief aggregator the
optimizer hot path can call with zero SDK calls. Honesty flags in MOCKS.md and docstrings.
</success_criteria>

<output>
After completion, create `.planning/phases/02-optimizer-core/02-02-SUMMARY.md`.
</output>
