---
phase: 03-web-app-decision-ui
plan: "03"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - requirements.txt
  - README.md
  - coolspend/tests/test_requirements.py
autonomous: true
requirements: [APP-02]
user_setup:
  - service: huggingface
    why: "Public deploy of the Gradio Space (judges reward a live web app) — final user step"
    env_vars:
      - name: INFRARED_API_KEY
        source: "infrared.city hackathon dashboard (issued May 27) — set as a Space Secret for the live backend"
    dashboard_config:
      - task: "Create a new Gradio Space and push the repo (or connect the GitHub repo)"
        location: "huggingface.co -> New -> Space -> SDK: Gradio"
      - task: "Add INFRARED_API_KEY + INFRARED_BACKEND=live as Space Secrets (only for the live demo)"
        location: "Space -> Settings -> Variables and secrets"
must_haves:
  truths:
    - "requirements.txt pins every dependency the app + pipeline need (gradio, pymoo 0.6.1, shapely, geojson, matplotlib, numpy, infrared-sdk) and installs in a clean env"
    - "README.md has a Hugging Face Spaces header (title + sdk: gradio + app_file) so the repo deploys as a Gradio Space"
    - "README documents the exact deploy steps and the offline-mock vs live-key distinction"
  artifacts:
    - path: "requirements.txt"
      provides: "pinned HF-Spaces-friendly dependency set"
      contains: "pymoo==0.6.1"
    - path: "README.md"
      provides: "Spaces YAML header + deploy instructions + mock/live note"
      contains: "sdk: gradio"
    - path: "coolspend/tests/test_requirements.py"
      provides: "test asserting requirements.txt pins the required packages + README has the Spaces header"
      contains: "def test_"
  key_links:
    - from: "README.md (app_file)"
      to: "coolspend/app.py demo.launch()"
      via: "Spaces header app_file points at the Gradio entrypoint"
      pattern: "app_file"
    - from: "requirements.txt"
      to: "coolspend imports (pymoo/shapely/numpy/matplotlib/gradio/infrared-sdk)"
      via: "every imported third-party package is pinned"
      pattern: "gradio"
---

<objective>
Make the repo deploy-ready as a Hugging Face Gradio Space (APP-02): a pinned
`requirements.txt`, a `README.md` with the Spaces YAML header pointing at the
`app.py` entrypoint, exact deploy instructions, and the offline-mock vs live-key
distinction. A test pins the dependency contract so it cannot silently drift.

Purpose: Judges reward a live web app + visible real API calls. The actual public
deploy and the live key are the USER's final step (key issued May 27) — this plan
makes everything deploy-ready and documents the exact steps; it does not block on a
real browser or public deploy.
Output: requirements.txt, README.md (Spaces header + steps), test_requirements.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<deps_in_use>
<!-- Third-party packages the app + pipeline import (from the codebase). Pin all of these. -->
- pymoo (NSGA-II)            -> pin pymoo==0.6.1   (PROJECT.md constraint — exact pin)
- shapely (collision engine) -> coolspend/spatial_engine.py
- numpy                      -> optimizer / sdk_client / spatial_engine
- matplotlib                 -> app_viz.py before/after plot + optimizer pareto plot (Agg backend)
- gradio                     -> app.py UI (pin a 4.x release)
- geojson                    -> REQUIREMENTS SHIP-01 lists it (site fixtures are plain GeoJSON parsed via json; keep the pin for the documented stack)
- infrared-sdk               -> sdk_client live path (lazy import; needed only for live but listed for the Space)
</deps_in_use>

<spaces_header_format>
A Hugging Face Gradio Space reads a YAML front-matter block at the very top of
README.md:
```
---
title: CoolSpend Tree Budget Optimizer
emoji: 🌳
colorFrom: green
colorTo: blue
sdk: gradio
sdk_version: <match the gradio pin in requirements.txt>
app_file: coolspend/app.py
pinned: false
---
```
The Space runs `app_file`'s module-level `demo` and serves `demo.launch()`.
</spaces_header_format>
</context>

<tasks>

<task type="auto">
  <name>Task 1: requirements.txt — pin the HF-Spaces dependency set</name>
  <files>requirements.txt</files>
  <read_first>.planning/PROJECT.md (Tech stack constraint: pymoo 0.6.1, shapely, geojson, numpy, infrared-sdk, gradio), .planning/REQUIREMENTS.md (SHIP-01 list)</read_first>
  <action>
    Create requirements.txt at the repo root pinning exactly these packages, one per line, with version specifiers:
      - pymoo==0.6.1            (PROJECT.md hard constraint)
      - gradio==4.44.1          (a stable 4.x release; set sdk_version in README to match this exact value)
      - shapely>=2.0,<3.0
      - numpy>=1.26,<2.0        (numpy<2 avoids pymoo 0.6.1 ABI breakage)
      - matplotlib>=3.7,<4.0
      - geojson>=3.0,<4.0
      - infrared-sdk            (no pin — version unknown until May 27; document this in README; lazy-imported so absence does not break offline/mock)
    Add a one-line comment header noting the file targets Hugging Face Spaces (Gradio SDK) and that infrared-sdk is only needed for the live backend.
    Verify the pins do not conflict by installing into a throwaway venv in the verify step.
  </action>
  <verify>
    <automated>python -m venv .venv_req_check && .venv_req_check\Scripts\python -m pip install -r requirements.txt --quiet && .venv_req_check\Scripts\python -c "import pymoo, gradio, shapely, numpy, matplotlib, geojson; print('ok', pymoo.__version__)"</automated>
  </verify>
  <done>requirements.txt exists with pymoo==0.6.1 and a 4.x gradio pin; a clean venv installs all pins without conflict and imports them.</done>
</task>

<task type="auto">
  <name>Task 2: README.md Spaces header + deploy steps + mock/live note, and test_requirements.py contract</name>
  <files>README.md, coolspend/tests/test_requirements.py</files>
  <read_first>requirements.txt (the gradio pin value to mirror into sdk_version), coolspend/app.py existence (app_file target; entrypoint is module-level demo + demo.launch()), .planning/ROADMAP.md (Phase 3 success criteria)</read_first>
  <action>
    Create README.md at the repo root. The VERY FIRST lines must be the YAML Spaces header from <spaces_header_format>, with sdk_version set to the EXACT gradio version pinned in requirements.txt (4.44.1) and app_file: coolspend/app.py.

    After the header, add these sections:
      - "## CoolSpend — Tree Budget Optimizer": one-paragraph what-it-is (decision, not a heatmap; persona = city Chief Heat Officer) lifted faithfully from PROJECT.md core value. Do NOT fabricate metrics.
      - "## Run locally (offline, no API key)": exact commands —
          `pip install -r requirements.txt`
          `python -m coolspend.app`   (opens the Gradio UI on mock; no key required)
          and the headless smoke: `python -c "import coolspend.app as a; a.build_demo()"`.
      - "## Deploy to Hugging Face Spaces": exact steps — create a Gradio Space, push this repo (the README header makes it a Gradio Space; app_file=coolspend/app.py), the Space installs requirements.txt automatically. Note this is the USER's final step.
      - "## Live Infrared backend (real UTCI)": to switch from mock to live, set Space Secrets `INFRARED_BACKEND=live` and `INFRARED_API_KEY=<key issued May 27>`; the app then makes real Infrared SDK calls (visible in the call-log panel). State clearly: mock = NOT MEASURED DATA; live = real measured UTCI. Never commit the key.
      - "## Honesty / MOCKS note": one line pointing at MOCKS.md and stating the surrogate carries +/-4 degC uncertainty and the 12 degC cap is unsourced (do not hide). (Full MOCKS ledger is Phase 4 / SHIP-02 — link, do not duplicate.)

    Then create coolspend/tests/test_requirements.py asserting the deploy contract:
      - requirements.txt exists and its text contains "pymoo==0.6.1", "gradio==", "shapely", "numpy", "matplotlib", "geojson", "infrared-sdk".
      - README.md exists; its first non-empty line is "---" (YAML header start); the header block contains "sdk: gradio" and "app_file: coolspend/app.py" and an "sdk_version:" line.
      - The sdk_version value in README matches the gradio== pin in requirements.txt (parse both; assert equal) — prevents silent drift.
      - README mentions both "mock" and "live" and the string "INFRARED_API_KEY".
  </action>
  <verify>
    <automated>python -m pytest coolspend/tests/test_requirements.py -q</automated>
  </verify>
  <done>README.md starts with the Gradio Spaces YAML header (app_file=coolspend/app.py, sdk_version matches the gradio pin); deploy + mock/live steps documented; test_requirements.py passes asserting the dependency + Spaces-header contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo/README -> public Space | Deployed code + docs become public |
| Space Secrets -> running app | INFRARED_API_KEY provided as a Space Secret env var |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Info disclosure | README / repo | mitigate | README instructs key via Space Secrets only; never commit INFRARED_API_KEY; .gitignore already excludes outputs/ |
| T-03-10 | Tampering | dependency pins | mitigate | Pinned versions (pymoo==0.6.1, numpy<2, gradio==4.44.1) + a test asserting the pins prevent silent drift |
| T-03-11 | Repudiation | mock-vs-live in public demo | mitigate | README states mock=NOT MEASURED DATA, live=real; honesty/MOCKS note links the ledger |
</threat_model>

<verification>
- `python -m pip install -r requirements.txt` in a clean venv installs without conflict (Task 1 verify)
- `python -m pytest coolspend/tests/test_requirements.py -q` — pass
- README.md opens with the Gradio Spaces YAML header; `app_file: coolspend/app.py`; sdk_version == gradio pin
</verification>

<success_criteria>
- requirements.txt pins gradio + pymoo 0.6.1 + shapely + geojson + matplotlib + numpy + infrared-sdk and installs clean (APP-02 / SHIP-01 forward-compatible)
- README has a Spaces-compatible header (sdk: gradio, app_file) so the repo deploys as a Gradio Space (APP-02)
- Exact deploy steps + offline-mock vs live-key distinction documented; live deploy + key are the user's final step
- A test pins the dependency + Spaces-header contract against drift
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-app-decision-ui/03-03-SUMMARY.md`
</output>
