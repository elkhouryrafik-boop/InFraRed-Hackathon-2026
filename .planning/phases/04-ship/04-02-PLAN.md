---
phase: 04-ship
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DEMO_SCRIPT.md
  - SUBMISSION.md
  - CHANGELOG.md
autonomous: true
requirements: [SHIP-03]
must_haves:
  truths:
    - "DEMO_SCRIPT.md gives a shot-by-shot 2.5–3 min screencast script with a named user (Maria, Chief Heat Officer), a hook open, the live app running with a visible real API call, and a €-per-°C headline close"
    - "DEMO_SCRIPT.md contains the exact runnable commands for both the safe mock recording and the live real-numbers recording"
    - "SUBMISSION.md is a short project description explicitly hitting all four judging axes (technical depth, creativity, real-world impact, presentation)"
    - "SUBMISSION.md includes the GitHub repo line, a placeholder HF Spaces URL, and a one-line pitch"
    - "CHANGELOG.md summarizes what was built per phase, derived from the phase SUMMARYs"
    - "No real API key/secret appears in any submission asset; the video-capture step is explicitly noted as the user's final manual step"
  artifacts:
    - path: "DEMO_SCRIPT.md"
      provides: "Shot-by-shot screencast script + exact recording commands"
      contains: "Maria"
    - path: "SUBMISSION.md"
      provides: "Short submission-form description hitting all four judging axes + repo/URL/pitch"
      contains: "Technical depth"
    - path: "CHANGELOG.md"
      provides: "What-we-built summary derived from phase SUMMARYs"
      contains: "Phase"
  key_links:
    - from: "DEMO_SCRIPT.md commands"
      to: "coolspend entrypoints"
      via: "exact CLI commands"
      pattern: "python -m coolspend\\.(app|main)"
    - from: "SUBMISSION.md"
      to: "judging axes"
      via: "named sections"
      pattern: "Technical depth|Creativity|Real-world impact|Presentation"
---

<objective>
Produce the submission assets that turn the final deliverable (GitHub repo + demo + short description) into a 5-minute job for the user (SHIP-03): a shot-by-shot DEMO_SCRIPT.md built on the winning hackathon narrative (named Chief Heat Officer "Maria", a decision that changes, hook open, live app with a visible real API call, €-per-°C headline close), a SUBMISSION.md short description hitting all four judging axes, and a CHANGELOG.md derived from the phase SUMMARYs.

Purpose: The deliverable is judged on presentation and real-world impact. The agent cannot record video — but it can make recording trivial and write the submission text. No video file is produced here; capture is the user's explicit final step.
Output: DEMO_SCRIPT.md, SUBMISSION.md, CHANGELOG.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<ground_truth_facts>
<!-- Verified against shipped code + real outputs/top3_configurations.json on 2026-05-21. -->
<!-- Demo numbers and commands MUST match these — do not invent figures. -->

Entrypoints (exact, verified):
- Gradio app:  `python -m coolspend.app`   (mock by default, no key)
- CLI:         `python -m coolspend.main`  (mock by default)
- Live:        set env `INFRARED_BACKEND=live` and `INFRARED_API_KEY=<key>` then run either entrypoint.
  On PowerShell that is: `$env:INFRARED_BACKEND="live"; $env:INFRARED_API_KEY="<key>"; python -m coolspend.app`

Real decision-artifact numbers (outputs/top3_configurations.json, mock backend):
- 3 ranked configs, labels: MAX_THERMAL_RELIEF (rank 1), MAX_ECOLOGICAL (rank 2), BALANCED (rank 3)
- tree_count = 12 per config; delta_tmrt_c = 1.305 (surrogate, ±4°C); validated_utci_c = 39.59 (MOCK until live)
- headline KPI: config["cost_per_utci_degree"]["value"] ≈ 5957 EUR/°C (unit "EUR/degC", confidence MED, DECLARED sources)
- run_metadata: NSGA-II (pymoo 0.6.1), site "Plaça dels Àngels, Barcelona", seed 42
- NOTE: with the LIVE backend after May 27 the validated_utci_c and €/°C numbers will differ from the mock figures above. The script should quote the mock numbers for the safety take and instruct the user to read the LIVE numbers off-screen for the real take. Do not hardcode a fake "live" number.

Persona / narrative (from PROJECT.md + research): user = city Chief Heat Officer, name her "Maria". The decision that changes = where to spend a fixed tree-planting budget for the most °C of UTCI relief per euro. Winning pattern: decision > visualization; live web app + ~3-min narrative; open with a hook.

Honesty contract (MUST be respected in SUBMISSION + DEMO_SCRIPT):
- optimizer uses a thermal SURROGATE in the NSGA-II loop and validates the Top-3 with the real Infrared SDK (SimBudget cap = 3 live calls/run)
- mock numbers are labeled NOT MEASURED DATA
- €/°C cost constants are DECLARED / REQUIRES_VERIFICATION
Do not overclaim; do not present mock figures as measured.

Phase SUMMARYs to derive CHANGELOG from (read these):
- .planning/phases/01-foundation-sdk-boundary/01-0{1,2,3}-SUMMARY.md  (SDK boundary, spatial engine, cost model)
- .planning/phases/02-optimizer-core/02-0{1..5}-SUMMARY.md            (rules, surrogate, live backend, NSGA-II+Top-3, decision artifact)
- .planning/phases/03-web-app-decision-ui/03-0{1,2,3}-SUMMARY.md      (pipeline wrapper, Gradio UI, HF deploy)

Submission deliverable = GitHub repo + demo + short description. HF Spaces URL is a PLACEHOLDER (Space created at kickoff). GitHub repo URL also placeholder if not yet known — mark clearly as <FILL IN>.
</ground_truth_facts>

@.planning/phases/01-foundation-sdk-boundary/01-01-SUMMARY.md
@.planning/phases/02-optimizer-core/02-05-SUMMARY.md
@.planning/phases/03-web-app-decision-ui/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write DEMO_SCRIPT.md — shot-by-shot screencast + exact recording commands (SHIP-03)</name>
  <read_first>PROJECT.md (persona, judging axes, honesty contract); ground_truth_facts above; outputs/top3_configurations.json for the real headline numbers</read_first>
  <files>DEMO_SCRIPT.md</files>
  <action>
    Write a 2.5–3 min screencast script structured shot-by-shot. Required structure (use clear headings/sections):

    1. `# CoolSpend — Demo Script (2.5–3 min)` with a one-line runtime budget per section.
    2. `## The hook (0:00–0:20)` — open on the problem: Maria, a city Chief Heat Officer, has a fixed budget and a heatwave; "where do 12 trees buy the most cooling per euro?" State the stakes (real-world impact axis).
    3. `## Shot list` — a numbered table or list, each row = [timecode, what's on screen, what the presenter says, what to click]. Walk: open the live Gradio app → show the default Plaça dels Àngels polygon + budget slider + TOPSIS weight sliders → click Run → POINT OUT the visible real Infrared API call in the call-log panel (SimBudget cap = 3 calls) → show the before/after UTCI map → show the ranked allocation table (3 configs, MAX_THERMAL_RELIEF / MAX_ECOLOGICAL / BALANCED) → land on the €-per-°C headline.
    4. `## The close (≈2:20–3:00)` — the €-per-°C headline (the decision: rank-1 config, ~5957 EUR/°C from the mock artifact — note the LIVE number will differ) + a one-sentence vision (district-scale triage, any city polygon).
    5. `## Recording commands` — TWO exact, copy-pasteable blocks:
       - SAFE TAKE (mock, no key, always works): `python -m coolspend.app` then narrate over mock data — label on-screen that this is NOT MEASURED DATA.
       - LIVE TAKE (real numbers, after May 27 key): PowerShell `$env:INFRARED_BACKEND="live"; $env:INFRARED_API_KEY="<your-key>"; python -m coolspend.app` — read the real validated UTCI + €/°C off the screen; do NOT hardcode a fake live number in the script.
       Include the CLI alternative `python -m coolspend.main` for a terminal-only backup shot.
    6. `## Final manual step (USER)` — explicitly state: the agent does NOT record video; the user records the ~3-min screencast following this script and uploads it, then links it from SUBMISSION.md. Recommend recording the SAFE take first as insurance, then the LIVE take for real numbers.

    Honesty: when showing mock data, the script must instruct the presenter to acknowledge it is surrogate/mock (NOT MEASURED DATA) and that the Top-3 are validated with the real SDK on the live take. Never embed a real API key — use `<your-key>` placeholder.
  </action>
  <verify>
    <automated>$d = Get-Content DEMO_SCRIPT.md -Raw; if (($d -match 'Maria') -and ($d -match 'Chief Heat Officer') -and ($d -match 'python -m coolspend\.app') -and ($d -match 'INFRARED_BACKEND="live"') -and ($d -match '(?i)hook') -and ($d -match '(?i)per[- ]?°?C|EUR/.?C|euro') -and ($d -match '(?i)NOT MEASURED DATA') -and ($d -match '(?i)record') -and ($d -notmatch 'INFRARED_API_KEY="[A-Za-z0-9]{8,}"')) { Write-Output 'DEMO-OK' } else { Write-Error 'DEMO_SCRIPT missing required elements' }</automated>
  </verify>
  <acceptance_criteria>
    - Contains "Maria" and "Chief Heat Officer" (named persona)
    - Has a hook section, a shot list, and a €-per-°C close
    - Contains both exact commands: `python -m coolspend.app` (mock) and `$env:INFRARED_BACKEND="live"` (live)
    - States mock data is NOT MEASURED DATA and that Top-3 are SDK-validated on the live take
    - Explicitly names video capture as the USER's final manual step
    - No literal API key value (placeholder `<your-key>` only)
  </acceptance_criteria>
  <done>Shot-by-shot 2.5–3 min script with named persona, hook, visible-real-call beat, €/°C close, both recording commands, and the user-records-video note; no embedded secret.</done>
</task>

<task type="auto">
  <name>Task 2: Write SUBMISSION.md — short description hitting all four judging axes (SHIP-03)</name>
  <read_first>PROJECT.md (judging axes + honesty contract); ground_truth_facts above</read_first>
  <files>SUBMISSION.md</files>
  <action>
    Write the short project description for the submission form. Required structure:

    1. `# CoolSpend` + a `## One-line pitch` (single sentence): a budget→decision tool that tells a city where to plant trees for the most °C of UTCI relief per euro, proved with real Infrared UTCI on the top picks.
    2. `## What it is` — 3–5 sentences: polygon + budget in, ranked tree-planting allocation + before/after UTCI map out; a decision, not a heatmap; built on the infrared.city SDK (Tree Budget track).
    3. Four explicitly-headed sections, one per judging axis (use these exact phrases as headings so they are grep-verifiable):
       - `## Technical depth` — NSGA-II (pymoo 0.6.1) 2-objective optimization over 12 tree coordinates with a budget constraint; analytical thermal surrogate in the hot loop (no per-chromosome SDK calls); Top-3 validated with the real Infrared UTCI SDK behind a SimBudget guard (cap 3 calls); TOPSIS €/°C ranking; deterministic JSON decision artifact; clean mock|cached|live SDK boundary; offline-testable.
       - `## Creativity` — reframes "heat map" as "budget allocation decision"; surrogate-optimize-then-validate pattern keeps it within sim budget; €-per-°C as the single defensible KPI.
       - `## Real-world impact` — the Chief Heat Officer (Maria) use case; defensible spend under a fixed budget during heatwaves; generalizes to any city polygon / district triage.
       - `## Presentation` — live Gradio app on Hugging Face Spaces; visible real API calls; ~3-min narrated demo; self-contained README + honesty MOCKS ledger.
    4. `## Honesty note` — state the surrogate-in-loop + real-SDK-Top-3-validation, mock = NOT MEASURED DATA, €/°C constants DECLARED/REQUIRES_VERIFICATION; link to MOCKS.md.
    5. `## Links` — `- GitHub repo: <FILL IN>`, `- Hugging Face Space: <FILL IN — created at kickoff>`, `- Demo video: <FILL IN after recording>`.

    Do not overclaim (honesty contract). No API key/secret anywhere.
  </action>
  <verify>
    <automated>$s = Get-Content SUBMISSION.md -Raw; if (($s -match '## Technical depth') -and ($s -match '## Creativity') -and ($s -match '## Real-world impact') -and ($s -match '## Presentation') -and ($s -match '(?i)one-line pitch') -and ($s -match 'GitHub') -and ($s -match '(?i)hugging face') -and ($s -match 'MOCKS') -and ($s -match '(?i)NOT MEASURED DATA|DECLARED')) { Write-Output 'SUBMISSION-OK' } else { Write-Error 'SUBMISSION missing axes/links/honesty' }</automated>
  </verify>
  <acceptance_criteria>
    - Contains all four axis headings: `## Technical depth`, `## Creativity`, `## Real-world impact`, `## Presentation`
    - Has a one-line pitch section
    - Links section includes GitHub repo line + Hugging Face Space placeholder + demo video placeholder
    - Honesty note present (NOT MEASURED DATA / DECLARED) and links to MOCKS.md
    - No literal API key/secret string
  </acceptance_criteria>
  <done>Short description covers all four judging axes explicitly, includes pitch + repo/Space/video links, respects honesty contract.</done>
</task>

<task type="auto">
  <name>Task 3: Write CHANGELOG.md — what-we-built summary from phase SUMMARYs (SHIP-03)</name>
  <read_first>All phase SUMMARYs under .planning/phases/0{1,2,3}-*/*-SUMMARY.md</read_first>
  <files>CHANGELOG.md</files>
  <action>
    Read the phase SUMMARYs (Phase 1: 3 plans; Phase 2: 5 plans; Phase 3: 3 plans) and write a concise CHANGELOG / "what we built" document grounded in what was actually shipped.

    Structure:
    1. `# CoolSpend — Changelog` with a one-line project description.
    2. One `## Phase N — <name>` section per phase, each with 3–6 bullets summarizing the real artifacts/decisions from that phase's SUMMARYs:
       - Phase 1: clean dedup sdk_client (mock|cached|live + SimBudget), spatial_engine (load_site, is_valid_location, single CRS boundary), cost_model (CapEx+OpEx → €/°C).
       - Phase 2: rules_engine (spacing penalty + species diversity), thermal surrogate (porosity-bug fixed), live Infrared backend wiring, NSGA-II 2-objective + Top-3 validation, TOPSIS €/°C decision artifact + main.py CLI.
       - Phase 3: app_pipeline.run_decision + app_viz before/after map, Gradio Blocks UI, HF Spaces deployability (pinned requirements + Spaces header).
    3. `## Test coverage` — note the offline test suite (test_*.py under coolspend/tests/) per STATE.md (15+122 tests green) — phrase as "per project records" without inventing exact new counts.
    4. Keep it factual and tied to shipped modules; do NOT claim features that aren't in the code (no scaffold modules, no pollinator 3rd objective — shipped as 2-objective).

    No secrets.
  </action>
  <verify>
    <automated>$c = Get-Content CHANGELOG.md -Raw; if (($c -match '(?i)## Phase 1') -and ($c -match '(?i)## Phase 2') -and ($c -match '(?i)## Phase 3') -and ($c -match 'sdk_client') -and ($c -match 'NSGA-II') -and ($c -match '(?i)gradio')) { Write-Output 'CHANGELOG-OK' } else { Write-Error 'CHANGELOG missing phases/modules' }</automated>
  </verify>
  <acceptance_criteria>
    - Contains `## Phase 1`, `## Phase 2`, `## Phase 3` sections
    - References real shipped modules (sdk_client, NSGA-II, gradio) — no scaffold-module or 3-objective claims
    - Test-coverage note phrased per project records (no fabricated counts)
    - No secrets
  </acceptance_criteria>
  <done>Per-phase what-we-built summary grounded in SUMMARYs and shipped modules, no overclaiming.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| submission assets → public | DEMO_SCRIPT / SUBMISSION / CHANGELOG are world-readable on the repo |
| claims → judges | Submission text must not overstate accuracy (honesty contract) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | DEMO_SCRIPT.md / SUBMISSION.md | mitigate | API key shown only as `<your-key>` placeholder + HF Space Secret; verify no literal key pattern before commit. |
| T-04-05 | Repudiation / honesty | SUBMISSION + DEMO_SCRIPT claims | mitigate | Mock = NOT MEASURED DATA; surrogate-in-loop + Top-3 real-SDK validation stated; €/°C constants DECLARED; no fabricated live numbers. |
| T-04-06 | Spoofing | demo "live" numbers | mitigate | Script instructs reading real numbers off-screen on the live take; no hardcoded fake live figures. |
</threat_model>

<verification>
- DEMO_SCRIPT.md: named persona, hook, shot list, visible-real-call beat, €/°C close, both mock+live commands, user-records-video note, no key literal
- SUBMISSION.md: all four axis headings, pitch, repo/Space/video links, honesty note, no key literal
- CHANGELOG.md: three phase sections grounded in shipped modules, no overclaiming
</verification>

<success_criteria>
- SHIP-03 assets complete: recording is a 5-minute job for the user (script + exact commands), submission text hits all four judging axes, changelog reflects real build
- Honesty contract respected throughout; no embedded secrets; video capture flagged as user's final step
</success_criteria>

<output>
After completion, create `.planning/phases/04-ship/04-02-SUMMARY.md`
</output>
