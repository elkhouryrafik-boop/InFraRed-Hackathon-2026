---
phase: 05-surrogate-ground-truth-honesty-reset
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - coolspend/spatial_engine.py
  - coolspend/optimizer.py
  - coolspend/sdk_client.py
  - coolspend/tests/test_coordinate_frame.py
  - requirements.txt
autonomous: true
requirements: [VALID-05]
must_haves:
  truths:
    - "All geometry runs through one projected metric CRS (UTM 31N / EPSG:32631) end-to-end"
    - "Site origin is computed per-site from the polygon's UTM bounding box (NOT a hardcoded centroid)"
    - "The optimizer keeps working in a [0,width]x[0,depth] local-metre frame derived from UTM"
    - "A WGS84->UTM->WGS84 round-trip assertion (<1 m) fires before every live SDK call and aborts the call on mismatch"
  artifacts:
    - path: "coolspend/spatial_engine.py"
      provides: "pyproj UTM-31N CRS boundary + per-site origin + round-trip guard helper"
      contains: "EPSG:32631"
    - path: "coolspend/tests/test_coordinate_frame.py"
      provides: "round-trip <1m test + per-site origin test + fail-closed guard test"
      contains: "assert_crs_roundtrip"
    - path: "requirements.txt"
      provides: "pyproj pin"
      contains: "pyproj"
  key_links:
    - from: "coolspend/sdk_client.py::_live_utci"
      to: "spatial_engine.assert_crs_roundtrip"
      via: "guard call before InfraredClient run"
      pattern: "assert_crs_roundtrip"
    - from: "coolspend/optimizer.py::_config_to_geometry"
      to: "spatial_engine.local_m_to_latlon"
      via: "polygon_lonlat construction unchanged"
      pattern: "local_m_to_latlon"
---

<objective>
Replace the single-anchor equirectangular cos-latitude CRS boundary in `spatial_engine.py`
with a proper pyproj UTM-31N (EPSG:32631) projection so a site can be placed anywhere in
Barcelona (the current ±200 m-of-centroid approximation breaks citywide). Preserve the
SW-corner local-metre frame `[0, width] x [0, depth]` (derived per-site from UTM offsets)
so the NSGA-II optimizer and shapely collision code keep working unchanged. Add a
WGS84->UTM->WGS84 round-trip consistency assertion (<1 m) that fires — and FAILS CLOSED —
immediately before every live SDK call (the coordinate footgun's exact danger point).

Purpose: VALID-05. This is the projection foundation the calibration study (Plan 03) and
all live calls depend on. Honors D-06 (pyproj EPSG:32631, per-site origin, preserved local
frame, no NSGA-II bound rewrite) and D-07 (<1 m round-trip guard before every live call).
Output: a citywide-correct CRS boundary + fail-closed live-call guard + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-surrogate-ground-truth-honesty-reset/05-CONTEXT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Current single-CRS boundary in coolspend/spatial_engine.py (TO REPLACE). -->
<!-- These two functions are the ONLY CRS conversion in coolspend (SPATIAL-03). -->
<!-- Callers: optimizer._config_to_geometry / _build_baseline_geometry / load_site / -->
<!-- sdk_client._live_utci (when geometry has polygon_local_m). They import these names -->
<!-- and MUST keep working unchanged. Keep the SAME function names and signatures. -->

```python
# coolspend/spatial_engine.py — current (equirectangular, single-anchor)
SITE_WIDTH_M: float = 60.0
SITE_DEPTH_M: float = 42.0
SITE_ORIGIN_LON: float = 2.1670   # hardcoded plaza centroid — REMOVE the hardcode dependency
SITE_ORIGIN_LAT: float = 41.3826
def latlon_to_local_m(lon, lat) -> tuple[float, float]: ...   # KEEP NAME + SIGNATURE
def local_m_to_latlon(x_m, y_m) -> tuple[float, float]: ...   # KEEP NAME + SIGNATURE
def load_site(path=None) -> dict: ...   # parses GeoJSON, converts coords via latlon_to_local_m
```

```python
# coolspend/sdk_client.py::_live_utci — guard insertion point (after polygon built, before SDK call)
# Builds `ring` (list of [lon,lat]) then:  with InfraredClient() as client: ...
# INSERT the round-trip guard right BEFORE `with InfraredClient()`.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pyproj and migrate the CRS boundary to UTM-31N with per-site origin</name>
  <read_first>
    - coolspend/spatial_engine.py (the latlon_to_local_m / local_m_to_latlon / load_site functions + SITE_ORIGIN_* constants being replaced)
    - requirements.txt
    - .planning/phases/05-surrogate-ground-truth-honesty-reset/05-CONTEXT.md (D-06)
  </read_first>
  <behavior>
    - Round-trip: local_m_to_latlon(latlon_to_local_m(lon, lat)) recovers (lon, lat) within <1 m for any Barcelona point (test 41.3826,2.1670 plaza AND a far point e.g. 41.42,2.18 Sagrada-area).
    - Per-site origin: after set_site_origin_from_polygon(ring_lonlat), latlon_to_local_m maps the polygon's SW UTM corner to (0,0) and points stay in [0, width]x[0, depth].
    - A site placed at a DIFFERENT Barcelona location (not the plaza) round-trips just as accurately (proves the single-anchor limitation is gone).
    - load_site() of the existing angels_site.geojson still returns boundary/buildings/streets as shapely geoms with positive area in the local frame (no regression).
  </behavior>
  <action>
    Add `pyproj>=3.6,<4` to requirements.txt, then `pip install "pyproj>=3.6,<4"` (pyproj is NOT currently installed — confirmed). 

    In spatial_engine.py, replace the equirectangular conversion with pyproj UTM-31N:
    - Build a module-level cached transformer pair:
      `from pyproj import Transformer`
      `_TO_UTM = Transformer.from_crs("EPSG:4326", "EPSG:32631", always_xy=True)`
      `_TO_WGS = Transformer.from_crs("EPSG:32631", "EPSG:4326", always_xy=True)`
      (always_xy=True → call order is (lon, lat) and returns (easting, northing) — match this everywhere).
    - Introduce a per-site origin module state set from the site polygon's UTM bbox:
      `_SITE_ORIGIN_E: float | None = None` and `_SITE_ORIGIN_N: float | None = None`
      (the UTM easting/northing of the polygon's SW corner = min(E), min(N) over the ring).
      Add `def set_site_origin_from_polygon(ring_lonlat: list[tuple[float,float]]) -> tuple[float,float]:`
      that transforms every (lon,lat) in the ring via `_TO_UTM`, sets `_SITE_ORIGIN_E=min(eastings)`,
      `_SITE_ORIGIN_N=min(northings)`, ALSO sets `SITE_WIDTH_M = max(E)-min(E)` and
      `SITE_DEPTH_M = max(N)-min(N)` (module-level, so the optimizer bounds follow the site), and
      returns (width_m, depth_m). DO NOT hardcode SITE_ORIGIN_LON/LAT as the origin (D-06).
    - Keep SITE_ORIGIN_LON/LAT as a DEFAULT fallback only: if `set_site_origin_from_polygon` was never
      called (e.g. legacy callers), lazily initialize the origin from the bundled angels_site.geojson
      boundary the first time a conversion runs, so existing tests/optimizer still work. Document this
      fallback in the docstring as "default site origin; real sites call set_site_origin_from_polygon".
    - Rewrite `latlon_to_local_m(lon, lat)`: `e, n = _TO_UTM.transform(lon, lat)`; ensure origin
      initialized; return `(e - _SITE_ORIGIN_E, n - _SITE_ORIGIN_N)`. (SW corner = (0,0); local metres
      are true UTM offsets, sub-metre distortion across Barcelona.)
    - Rewrite `local_m_to_latlon(x_m, y_m)`: ensure origin initialized; `lon, lat = _TO_WGS.transform(x_m + _SITE_ORIGIN_E, y_m + _SITE_ORIGIN_N)`; return `(lon, lat)`.
    - Update `load_site()` so that BEFORE converting feature coords it calls
      `set_site_origin_from_polygon` with the site_boundary ring (so the origin is the site's own SW
      corner, per-site). Keep the rest of load_site (caching, shapely build) unchanged.
    - Update module + function docstrings: remove "equirectangular cos-lat / ±200 m of plaza centroid"
      language; state "UTM-31N (EPSG:32631) via pyproj; valid across Barcelona; per-site SW-corner local frame".
    - DO NOT touch the NSGA-II decision-variable bounds construction in optimizer.py — they read
      SITE_WIDTH_M/SITE_DEPTH_M which now follow the site automatically.
  </action>
  <verify>
    <automated>python -c "import pyproj; print(pyproj.__version__)"</automated>
    <automated>pytest coolspend/tests/test_coordinate_frame.py -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "EPSG:32631" coolspend/spatial_engine.py returns >= 1
    - grep -c "pyproj" requirements.txt returns 1
    - grep -c "always_xy=True" coolspend/spatial_engine.py returns >= 1
    - grep -c "def set_site_origin_from_polygon" coolspend/spatial_engine.py returns 1
    - grep -c "equirectangular" coolspend/spatial_engine.py returns 0
    - `python -c "import pyproj"` exits 0
    - pytest coolspend/tests/test_coordinate_frame.py exits 0
  </acceptance_criteria>
  <done>UTM-31N pyproj boundary live; per-site origin from polygon bbox; local frame preserved; angels_site.geojson load + existing optimizer bounds unaffected; no equirectangular code remains.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail-closed round-trip guard fired before every live SDK call</name>
  <read_first>
    - coolspend/spatial_engine.py (the new conversion functions from Task 1)
    - coolspend/sdk_client.py::_live_utci (the polygon-build then InfraredClient call site)
    - coolspend/tests/test_coordinate_frame.py
    - .planning/phases/05-surrogate-ground-truth-honesty-reset/05-CONTEXT.md (D-07)
  </read_first>
  <behavior>
    - assert_crs_roundtrip(ring_lonlat) passes (returns max error in metres) when every vertex round-trips < 1 m.
    - assert_crs_roundtrip raises CRSConsistencyError (a defined exception, fail-closed) when any vertex round-trips >= 1.0 m, and the raised message names the worst error in metres.
    - _live_utci aborts (raises, no SDK call) when the guard fails — the InfraredClient call is unreachable past a guard failure.
  </behavior>
  <action>
    In spatial_engine.py add a fail-closed guard (D-07):
    - `class CRSConsistencyError(RuntimeError): ...`
    - `def assert_crs_roundtrip(ring_lonlat: list[tuple[float,float]], tol_m: float = 1.0) -> float:`
      For each (lon,lat): compute x,y = latlon_to_local_m(lon,lat); lon2,lat2 = local_m_to_latlon(x,y);
      measure the ground error in METRES by transforming both (lon,lat) and (lon2,lat2) to UTM via
      `_TO_UTM` and taking the euclidean distance (NOT degrees — metres). Track the max error.
      If max_error_m >= tol_m raise CRSConsistencyError(f"CRS round-trip {max_error_m:.3f} m exceeds {tol_m} m — aborting live call (D-07)"). Otherwise return max_error_m.
    - The guard MUST fail closed: any exception path leaves the SDK uncalled.
    In sdk_client.py::_live_utci, immediately AFTER `ring` is built and closed and BEFORE
    `with InfraredClient() as client:`, insert a local import + guard call:
      `from coolspend.spatial_engine import assert_crs_roundtrip  # noqa: PLC0415`
      `assert_crs_roundtrip(ring)`  (ring is the list of [lon,lat] pairs).
    Keep the lazy-import pattern (inside the function, never module top) per established convention.
    Do NOT log coordinates containing the API key path; the guard only touches geometry, no secrets.
  </action>
  <verify>
    <automated>pytest coolspend/tests/test_coordinate_frame.py -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def assert_crs_roundtrip" coolspend/spatial_engine.py returns 1
    - grep -c "class CRSConsistencyError" coolspend/spatial_engine.py returns 1
    - grep -c "assert_crs_roundtrip" coolspend/sdk_client.py returns >= 1
    - test_coordinate_frame.py contains a test asserting CRSConsistencyError is raised on a >1m mismatch (e.g. test_guard_fails_closed)
    - test_coordinate_frame.py contains a test asserting the guard runs before the SDK (e.g. monkeypatch InfraredClient to fail if reached, force a bad ring, assert CRSConsistencyError not the SDK error)
    - pytest coolspend/tests/test_coordinate_frame.py exits 0
  </acceptance_criteria>
  <done>assert_crs_roundtrip measures error in metres, raises CRSConsistencyError >=1 m, and is called before every live Infrared call; SDK unreachable on guard failure; tests cover pass + fail-closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coolspend -> Infrared live API | Geometry (WGS84 polygon) crosses to the paid external API; INFRARED_API_KEY read from env |
| user polygon -> CRS boundary | Untrusted lon/lat input projected to UTM; bad coords could produce spatially-wrong UTCI silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | spatial_engine CRS conversion | mitigate | assert_crs_roundtrip (<1 m, in metres) fails closed before every live call — wrong-CRS coords cannot silently reach the API (D-07) |
| T-05-02 | Information disclosure | sdk_client guard insertion | mitigate | Guard touches only geometry; INFRARED_API_KEY never read, logged, or formatted in this plan's code path |
| T-05-03 | Denial of service (wallet) | live call reachability | accept | Live-call budget capping is owned by SimBudget (Plan 03); this plan only adds a pre-call correctness guard, does not change call counts |
</threat_model>

<verification>
- `pip install -r requirements.txt` resolves with pyproj pinned.
- pytest coolspend/tests/test_coordinate_frame.py passes (round-trip <1 m, per-site origin, fail-closed guard).
- pytest coolspend/tests/test_spatial_engine.py and test_optimizer.py still pass (no local-frame regression).
- grep confirms no "equirectangular" code remains and EPSG:32631 is the projection.
</verification>

<success_criteria>
All geometry runs through UTM-31N end-to-end; site origin is per-site from polygon UTM bbox;
the optimizer's [0,width]x[0,depth] frame is preserved (no NSGA-II bound rewrite); a <1 m
round-trip assertion fires and fails closed before every live SDK call.
</success_criteria>

<output>
After completion, create `.planning/phases/05-surrogate-ground-truth-honesty-reset/05-01-SUMMARY.md`
</output>
