---
phase: 05-surrogate-ground-truth-honesty-reset
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - coolspend/cost_model.py
  - coolspend/tests/test_cost_model.py
autonomous: true
requirements: [VALID-02, VALID-04]
must_haves:
  truths:
    - "The euro/degC KPI is computed from a true UTCI-hours delta (surrogate ΔTmrt routed through utci_hours_above()), never from raw Tmrt"
    - "The KPI reports BOTH euro/degC (mean-UTCI degC drop) AND euro per UTCI-hour reduced"
    - "The KPI is reported as an uncertainty interval [lo, hi], never a bare point estimate"
    - "Before a calibration artifact exists, the band is labelled 'pre-calibration, assumed ±4°C' (not implying empirical grounding)"
  artifacts:
    - path: "coolspend/cost_model.py"
      provides: "Tmrt->UTCI-routed KPI with both units + [lo,hi] band"
      contains: "utci_hours_above"
    - path: "coolspend/tests/test_cost_model.py"
      provides: "tests for UTCI routing, both units, band interval, pre-calibration label"
      contains: "value_lo"
  key_links:
    - from: "coolspend/cost_model.py::cost_per_utci_degree"
      to: "nature_metrics.utci_hours_above"
      via: "surrogate ΔTmrt -> coverage -> UTCI-hours delta before forming KPI"
      pattern: "utci_hours_above"
---

<objective>
Stop the headline KPI from being raw Tmrt over euros. Route the surrogate ΔTmrt (via the
config's coverage_fraction) through the existing `nature_metrics.py::utci_hours_above()`
ladybug UTCI path BEFORE forming the €/°C KPI, so the KPI reports true UTCI — not raw Tmrt
(D-08). Report BOTH units: the primary €/°C headline (UTCI-hours-above-threshold reduction
converted to an equivalent mean-UTCI °C drop, preserving the v1 framing) AND a secondary
€ per UTCI-hour reduced (annual hours above 32 °C avoided) (D-09). Report every KPI as an
uncertainty interval `[lo, hi]` propagated from the surrogate band, never a bare point
estimate (D-10/VALID-04). Before a calibration artifact exists, label the band
"pre-calibration, assumed ±4°C" rather than implying empirical grounding (D-10).

Purpose: VALID-02 (UTCI not Tmrt) + VALID-04 (uncertainty interval). This is unit-correctness
only — the cost-magnitude fix (€350/€35 → fully-loaded) is Phase 6, NOT this plan.
Output: a UTCI-routed, dual-unit, interval-reported KPI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-surrogate-ground-truth-honesty-reset/05-CONTEXT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Existing functions this plan wires together. Use them directly. -->

```python
# coolspend/cost_model.py — current KPI (TO EXTEND, keep function name + dict contract)
CAPEX_PER_TREE_EUR = 350.0   # DECLARED placeholder — magnitude fix is Phase 6, DO NOT change here
def total_cost(config) -> float: ...    # tree_count * (CapEx + OpEx*horizon)
def cost_per_utci_degree(config) -> dict:
    # returns {value, unit, confidence, sources, note, metric_id}
    # currently: prefers config["delta_utci_c"] else falls back to raw config["delta_tmrt_c"]  <-- THE BUG
```

```python
# nature_metrics.py::utci_hours_above (line 125) — the UTCI conversion path (D-08)
def utci_hours_above(threshold_c: float = 32.0, coverage_fraction: float = 0.0) -> dict:
    # returns {"value": hours, "unit": "hours / yr with UTCI > {t}°C",
    #          "baseline_value": baseline_hours, "delta": -reduction, "confidence": ..., ...}
    # delta is negative = reduction in annual hours above threshold for that coverage_fraction.
```

```python
# Config dicts carry (from optimizer.select_top3 / validate_top3_with_infrared):
#   config["delta_tmrt_c"]            surrogate ΔTmrt (°C)  — DO NOT use raw as the KPI delta
#   config["delta_utci_c"]            validated UTCI delta (°C) from real/cached Infrared
#   config["coverage_fraction"]       (may be absent; if so derive from trees — see action)
#   config["tree_count"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route the KPI through utci_hours_above and report both units</name>
  <read_first>
    - coolspend/cost_model.py (cost_per_utci_degree + total_cost — the whole file)
    - nature_metrics.py lines 90-220 (utci_hours_above + _derive_hourly_tmrt)
    - coolspend/tests/test_cost_model.py
    - .planning/phases/05-surrogate-ground-truth-honesty-reset/05-CONTEXT.md (D-08, D-09)
  </read_first>
  <behavior>
    - For a config with coverage_fraction>0 and NO validated delta_utci_c: the KPI delta is derived from utci_hours_above (reduction in annual UTCI-hours above 32°C), NOT from raw config["delta_tmrt_c"].
    - Result dict gains `utci_hours_reduced` (annual hours above 32°C avoided, positive) and `cost_per_utci_hour` (€ / hour reduced).
    - Result dict's primary `value` is € per equivalent mean-UTCI °C drop (see conversion below), unit "EUR/degC".
    - A config with validated delta_utci_c still PREFERS the measured value for the °C drop (confidence MED/HIGH) but ALSO reports cost_per_utci_hour from utci_hours_above for consistency.
    - Zero/negative reduction returns value=None with an explanatory note (existing guard preserved).
  </behavior>
  <action>
    Extend cost_per_utci_degree(config) in cost_model.py (KEEP the function name and the existing
    dict keys; ADD new keys). Steps:
    1. Determine coverage_fraction: `cov = config.get("coverage_fraction")`; if None, compute it from
       the active trees via `from coolspend.spatial_engine import core_weighted_coverage_fraction` over
       `[t for t in config.get("trees",[]) if t.get("active", True)]` (local import, noqa: PLC0415).
    2. Call `from nature_metrics import utci_hours_above` (local import). `uh = utci_hours_above(32.0, cov)`.
       `hours_reduced = -uh.get("delta", 0.0)` (delta is negative = reduction → flip sign to positive).
    3. Equivalent mean-UTCI °C drop (D-09 conversion — Claude's discretion, document it):
       Use the prefer-measured rule: if `config.get("delta_utci_c")` is a positive float, the °C drop =
       that measured value (confidence MED/HIGH). Otherwise derive the equivalent mean-°C drop from the
       UTCI-hours reduction so the headline never uses raw Tmrt: define
       `equiv_degc = hours_reduced / HOURS_PER_DEGC_REF` where you add a documented module constant
       `HOURS_PER_DEGC_REF: float` = the baseline annual UTCI-hours-above-32 divided by the baseline UTCI
       margin (compute `baseline_hours = uh.get("baseline_value")`; pick a defensible, documented divisor
       — e.g. equiv_degc = hours_reduced scaled so a full surrogate ΔTmrt cap maps to a few °C UTCI,
       consistent with the "12°C Tmrt ≈ 3-5°C UTCI" note in STATE). Tag this conversion in the result
       `note` as "UTCI-hours→equiv mean-°C conversion (documented, surrogate-derived)". The KEY invariant
       under test: the headline NEVER divides cost by raw config["delta_tmrt_c"].
    4. `cost = total_cost(config)`. Primary: `value = round(cost / equiv_degc, 2)` if equiv_degc>0 else None.
       Secondary: `cost_per_utci_hour = round(cost / hours_reduced, 2)` if hours_reduced>0 else None.
    5. Return the dict extended with: `utci_hours_reduced` (float|None), `cost_per_utci_hour` (float|None),
       `utci_hours_unit: "EUR / annual UTCI-hour above 32°C reduced"`, and KEEP value/unit("EUR/degC")/
       confidence/sources/note/metric_id. Update `note` to state both units and that the delta is UTCI
       (routed through utci_hours_above), never raw Tmrt.
    6. REMOVE the old fallback branch that set the KPI delta directly to raw `config["delta_tmrt_c"]`.
       Raw Tmrt may inform coverage but must never be the KPI's °C denominator.
    7. Guard: nature_metrics may return {"value": None, "error": ...} if EPW is unavailable. In that case
       set hours_reduced/cost_per_utci_hour to None and note "UTCI-hours unavailable (EPW missing)" but
       still attempt the measured-delta path; never crash.
    Do NOT change CAPEX_PER_TREE_EUR/OPEX constants — magnitude is Phase 6 (flag below).
  </action>
  <verify>
    <automated>pytest coolspend/tests/test_cost_model.py -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "utci_hours_above" coolspend/cost_model.py returns >= 1
    - grep -c "cost_per_utci_hour" coolspend/cost_model.py returns >= 1
    - test_cost_model.py contains a test proving the KPI delta is NOT raw delta_tmrt_c (e.g. a config with delta_tmrt_c=12 and coverage giving a small UTCI-hours reduction yields a value far larger than 1_000_000/12)
    - test_cost_model.py contains a test asserting cost_per_utci_hour is present and finite for a positive-reduction config
    - pytest coolspend/tests/test_cost_model.py exits 0
  </acceptance_criteria>
  <done>KPI routes ΔTmrt through utci_hours_above before forming €/°C; both units (€/°C and € per UTCI-hour) reported; raw-Tmrt-as-denominator path removed; cost constants untouched (Phase 6).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Report the KPI as an uncertainty interval [lo, hi] with pre-calibration labelling</name>
  <read_first>
    - coolspend/cost_model.py (the extended cost_per_utci_degree from Task 1)
    - coolspend/tests/test_cost_model.py
    - .planning/phases/05-surrogate-ground-truth-honesty-reset/05-CONTEXT.md (D-10, VALID-04)
  </read_first>
  <behavior>
    - The result dict carries `value_lo` and `value_hi` for the €/°C KPI (and `cost_per_utci_hour_lo/hi`), forming an interval that brackets `value`.
    - When no calibration band is supplied, the band uses ±4°C and `band_source` == "pre-calibration, assumed ±4°C".
    - When a calibration RMSE is supplied (band_c argument), `band_source` reflects the empirical RMSE and the interval widens/narrows accordingly.
    - The interval is never collapsed to a point: value_lo < value < value_hi whenever the band is nonzero and value is not None.
  </behavior>
  <action>
    Add a band parameter to the KPI (D-10/VALID-04). Signature:
    `def cost_per_utci_degree(config, band_c: float | None = None) -> dict:`
    - Add module constant `PRE_CALIBRATION_BAND_C: float = 4.0` with a comment that this is the
      pre-calibration assumed band (replaced by empirical calibration RMSE in Plan 03).
    - If `band_c is None`: `band = PRE_CALIBRATION_BAND_C`; `band_source = "pre-calibration, assumed ±4°C"`.
      Else: `band = band_c`; `band_source = f"empirical calibration RMSE ±{band_c:.2f}°C"`.
    - Propagate the band through the division: the °C drop has uncertainty ±band, so the KPI interval is
      `value_lo = round(cost / (degc_drop + band), 2)` (more cooling assumed → cheaper per °C → lower bound)
      and `value_hi = round(cost / max(degc_drop - band, EPS), 2)` (less cooling → costlier → upper bound;
      if degc_drop - band <= 0, value_hi = None with note "band exceeds estimate — upper bound unbounded").
      Use EPS=1e-6. Apply the SAME propagation shape to cost_per_utci_hour using a proportional hours band
      (scale band_c into hours via the same HOURS_PER_DEGC_REF factor used in Task 1) producing
      `cost_per_utci_hour_lo/hi`.
    - Add to the result dict: `value_lo`, `value_hi`, `cost_per_utci_hour_lo`, `cost_per_utci_hour_hi`,
      `band_c`, `band_source`. Update `note` to state the interval is never a bare point estimate.
    - When value is None (non-positive delta) set all *_lo/*_hi to None.
  </action>
  <verify>
    <automated>pytest coolspend/tests/test_cost_model.py -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "value_lo" coolspend/cost_model.py returns >= 1
    - grep -c "value_hi" coolspend/cost_model.py returns >= 1
    - grep -c "pre-calibration, assumed" coolspend/cost_model.py returns >= 1
    - grep -c "band_c" coolspend/cost_model.py returns >= 1
    - test_cost_model.py contains a test asserting value_lo < value < value_hi for a positive-delta config with default band
    - test_cost_model.py contains a test asserting band_source == "pre-calibration, assumed ±4°C" when band_c is None, and that passing band_c=2.0 changes band_source to mention empirical RMSE
    - pytest coolspend/tests/test_cost_model.py exits 0
  </acceptance_criteria>
  <done>KPI reported as [value_lo, value_hi] interval for both units; band defaults to assumed ±4°C labelled "pre-calibration"; passing an empirical RMSE relabels the band source; interval never a bare point.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cost_model -> nature_metrics (EPW) | Reads a local EPW climate file; no external network, no secrets |
| KPI output -> external copy/audit JSON | A bare point estimate would overstate certainty (honesty risk) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Repudiation/Spoofing (false certainty) | cost_per_utci_degree output | mitigate | KPI always reported as [lo,hi] interval with band_source labelled; bare point estimate impossible (VALID-04/D-10) |
| T-05-05 | Tampering (unit substitution) | KPI delta source | mitigate | Raw-Tmrt-as-denominator path removed; delta routed through utci_hours_above (D-08); test pins the invariant |
| T-05-06 | Information disclosure | nature_metrics EPW read | accept | EPW is public climate data, no PII/secrets; local file only |
</threat_model>

<verification>
- pytest coolspend/tests/test_cost_model.py passes (UTCI routing, both units, interval, pre-calibration label).
- grep confirms utci_hours_above is the delta source and value_lo/value_hi exist.
- KNOWN MOCK DEBT (flagged, not fixed here): CAPEX_PER_TREE_EUR=350 / OPEX_PER_TREE_YEAR_EUR=35 remain
  DECLARED placeholders — magnitude credibility is Phase 6 / COST-03 (owner: Phase 6). This plan fixes
  KPI UNIT-correctness only, per D-15's explicit-debt-flagging requirement.
</verification>

<success_criteria>
The €/°C KPI is computed from a true UTCI-hours delta (never raw Tmrt), reported in both
units (€/°C and € per UTCI-hour reduced), and always as an uncertainty interval [lo, hi]
with a band source label (assumed ±4°C pre-calibration, or empirical RMSE once available).
</success_criteria>

<output>
After completion, create `.planning/phases/05-surrogate-ground-truth-honesty-reset/05-02-SUMMARY.md`
</output>
