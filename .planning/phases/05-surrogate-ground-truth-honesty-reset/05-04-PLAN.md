---
phase: 05-surrogate-ground-truth-honesty-reset
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - coolspend/optimizer.py
  - coolspend/spatial_engine.py
  - coolspend/tests/test_optimizer.py
  - coolspend/tests/test_surrogate.py
autonomous: true
requirements: [HONEST-02, HONEST-03]
must_haves:
  truths:
    - "improvement_vs_naive_pct and the entire naive-baseline machinery are removed from code — no references survive"
    - "The artifact/audit JSON contains no 'validated with Infrared' / '88% vs naive' overclaim strings"
    - "The surrogate ceiling citation is re-anchored to Schrodi 2023 (arXiv:2310.05691, venue PENDING, ML/Tmrt-magnitude-only) + Rahman 2022, with Garcia-Nevado demoted to a shade-structure/surface-temp analogue"
    - "No fabricated DOIs are introduced; unverified refs are tagged PENDING"
  artifacts:
    - path: "coolspend/optimizer.py"
      provides: "naive-baseline machinery deleted; audit/artifact JSON honesty strings corrected"
      contains: "re-simulated with Infrared UTCI"
    - path: "coolspend/spatial_engine.py"
      provides: "surrogate docstring citation re-anchored to Schrodi 2023 + Rahman 2022"
      contains: "Schrodi"
  key_links:
    - from: "coolspend/optimizer.py::save_outputs"
      to: "the artifact JSON"
      via: "no improvement_vs_naive / no naive baseline / honest backend phrasing"
      pattern: "re-simulated"
---

<objective>
Remove the mock-vs-mock overclaims from the code-side honesty surfaces. Delete the entire
naive-baseline machinery and the "88% vs naive" figure (D-12): `_build_naive_baseline`,
`naive_baseline_config`, the `improvement_vs_naive_pct` computation and field, and every
reference. Replace "validated with Infrared" framing in the artifact/audit JSON with
"final picks re-simulated with Infrared UTCI" (HONEST-03/D-11 code surfaces). Re-anchor the
surrogate ceiling citation (HONEST-02/D-13): cite Schrodi 2023 (arXiv:2310.05691 — venue
PENDING, an ML method cited for the Tmrt MAGNITUDE anchor only, NOT implying we use their ML
approach) + Rahman 2022; demote Garcia-Nevado 2020 to a shade-structure/surface-temp analogue.
No fabricated DOIs — tag any unverified ref PENDING.

Purpose: HONEST-02 (citation re-anchor in code) + HONEST-03 (remove "validated"/"88%" from
code-emitted artifacts) + D-12 (delete naive baseline). The prose/doc surfaces (CONCEPT_REPORT,
README, MOCKS, app UI) are Plan 05 — this plan owns optimizer.py + spatial_engine.py code.
Output: clean code-emitted artifacts with honest phrasing and re-anchored citations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-surrogate-ground-truth-honesty-reset/05-CONTEXT.md
@.planning/ROADMAP.md

<interfaces>
<!-- TARGETS in coolspend/optimizer.py (to DELETE / REPHRASE) -->
```python
def naive_baseline_config() -> dict: ...                  # DELETE (D-12)
def _build_naive_baseline(budget=None) -> dict: ...       # DELETE (D-12)
# in save_outputs(): the "Naive baseline + improvement_vs_naive" block computing
#   improvement_vs_naive_pct, and artifact keys "baseline_naive" + "improvement_vs_naive_pct"  # DELETE
# in select_top3(): cfg["delta_tmrt_source"] = "...Garcia-Nevado 2020 surface-temp proxy..."   # RE-ANCHOR
# in write_audit_record(): "max_tmrt_reduction_c_cap": "12°C — UNSOURCED..."                    # RE-ANCHOR / honesty
```

<!-- TARGET in coolspend/spatial_engine.py (citation docstrings to RE-ANCHOR, D-13) -->
```python
# module docstring + delta_tmrt_surrogate docstring + MAX_TMRT_REDUCTION_C comment currently say:
#   "Garcia-Nevado 2020 (pavement IR thermography — surface temp, NOT Tmrt at 1.1m) + Vanos 2020"
#   "MAX_TMRT_REDUCTION_C = 12.0°C is an UNSOURCED hard-coded cap"
# RE-ANCHOR to Schrodi 2023 (arXiv:2310.05691, venue PENDING, ML method, Tmrt-magnitude anchor only)
#   + Rahman 2022 (tree-Tmrt anchor, DOI PENDING if unconfirmed); demote Garcia-Nevado to analogue.
```

<!-- Citation facts from 05-CONTEXT.md canonical_refs (use VERBATIM, no fabricated DOIs):
  - Schrodi et al. 2023 — arXiv:2310.05691 "Climate-sensitive Urban Planning through Optimization
    of Tree Placements". Venue attribution UNCONFIRMED → tag venue PENDING. ML method (NN-estimated
    Tmrt/SVF) → cite for the Tmrt MAGNITUDE anchor only; do NOT imply we use their ML approach.
  - Rahman et al. 2022 — supporting tree-Tmrt anchor; DOI PENDING if unconfirmed.
  - Garcia-Nevado 2020 — DEMOTE to shade-structure/surface-temp analogue.  -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete naive-baseline machinery and improvement_vs_naive_pct entirely (D-12)</name>
  <read_first>
    - coolspend/optimizer.py (naive_baseline_config, _build_naive_baseline, save_outputs naive block, artifact keys)
    - coolspend/tests/test_optimizer.py (any test referencing naive / improvement)
    - .planning/phases/05-surrogate-ground-truth-honesty-reset/05-CONTEXT.md (D-12)
  </read_first>
  <action>
    In coolspend/optimizer.py:
    - DELETE the `naive_baseline_config()` function in full.
    - DELETE the `_build_naive_baseline()` function in full.
    - In `save_outputs()`, DELETE the entire "Naive baseline + improvement_vs_naive (REMEDIATION fix #3)"
      block (the lines computing improvement_vs_naive_pct and `rank1["improvement_vs_naive_pct"] = ...`).
    - In the `artifact = {...}` dict in save_outputs, REMOVE the keys `"baseline_naive"` and
      `"improvement_vs_naive_pct"`.
    - Remove any now-unused imports/helpers that only the naive path used (verify nothing else references them).
    In coolspend/tests/test_optimizer.py:
    - Remove or rewrite any test that asserts on naive_baseline_config / _build_naive_baseline /
      improvement_vs_naive_pct so the suite passes; add a regression test
      `test_no_naive_baseline_in_artifact` that runs save_outputs (mock backend) and asserts the artifact
      JSON has NO "improvement_vs_naive_pct" and NO "baseline_naive" key.
    Run a repo-wide grep to confirm zero surviving references in coolspend/ (excluding planning docs).
  </action>
  <verify>
    <automated>pytest coolspend/tests/test_optimizer.py -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -rc "improvement_vs_naive" coolspend/ (excluding tests asserting absence) returns 0 in non-test source; specifically grep -c "improvement_vs_naive" coolspend/optimizer.py returns 0
    - grep -c "def naive_baseline_config" coolspend/optimizer.py returns 0
    - grep -c "def _build_naive_baseline" coolspend/optimizer.py returns 0
    - grep -c "baseline_naive" coolspend/optimizer.py returns 0
    - test_optimizer.py contains test_no_naive_baseline_in_artifact asserting the artifact lacks those keys
    - pytest coolspend/tests/test_optimizer.py exits 0
  </acceptance_criteria>
  <done>All naive-baseline code and the improvement_vs_naive_pct field/computation deleted; artifact no longer emits them; regression test pins their absence; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Re-anchor surrogate citation (Schrodi 2023 + Rahman 2022) and fix artifact/audit honesty strings</name>
  <read_first>
    - coolspend/spatial_engine.py (module docstring, delta_tmrt_surrogate docstring, MAX_TMRT_REDUCTION_C comment)
    - coolspend/optimizer.py (select_top3 delta_tmrt_source string; write_audit_record surrogate_flags; save_outputs disclaimers)
    - coolspend/tests/test_surrogate.py
    - .planning/phases/05-surrogate-ground-truth-honesty-reset/05-CONTEXT.md (D-13, D-11 audit/artifact JSON)
  </read_first>
  <action>
    Citation re-anchor (D-13) — in coolspend/spatial_engine.py:
    - Update the module docstring SURROGATE HONESTY block, the `delta_tmrt_surrogate` docstring HONESTY
      FLAGS, and the `MAX_TMRT_REDUCTION_C` comment to cite, VERBATIM intent:
      "Tmrt-magnitude anchor: Schrodi et al. 2023 (arXiv:2310.05691, venue PENDING) — tree-placement
      point-wise ΔTmrt; an ML method, cited for the Tmrt magnitude only (we do NOT use their ML approach,
      ML stays ruled out). Supporting: Rahman et al. 2022 (tree-Tmrt anchor, DOI PENDING). Garcia-Nevado
      2020 is a shade-structure / surface-temperature ANALOGUE only (measures pavement surface temp, not
      Tmrt at 1.1m), demoted from the primary anchor." Keep the ±4°C uncertainty note and the
      "still an unsourced linear cap — REQUIRES_VERIFICATION" honesty (re-anchoring the magnitude does NOT
      claim the 12°C cap is now sourced). NO fabricated DOIs; PENDING tags where unconfirmed.
    Artifact/audit honesty strings (D-11 code surfaces) — in coolspend/optimizer.py:
    - In `select_top3`, change `cfg["delta_tmrt_source"]` from the Garcia-Nevado surface-temp proxy string
      to: "analytical surrogate ΔTmrt; magnitude anchored to Schrodi 2023 (arXiv:2310.05691, PENDING) +
      Rahman 2022; Garcia-Nevado 2020 = surface-temp analogue. NOT measured/simulated. ±4°C."
    - In `select_top3`, change `cfg["surrogate_note"]` to honest re-simulation phrasing (HONEST-03):
      "Analytical proxy — final picks re-simulated below with real Infrared UTCI (not 'validated')."
    - In `write_audit_record`, update `surrogate_flags["max_tmrt_reduction_c_cap"]` and
      `data_sources["surrogate_physics"]` to reference the re-anchored citation + PENDING tags, and ensure
      no "validated with Infrared" phrasing; use "re-simulated with Infrared UTCI" where describing the
      Top-3 step.
    - In `save_outputs` `surrogate_disclaimer`, keep "SURROGATE-DERIVED ... ±4°C ... See MOCKS.md" but
      ensure it does NOT contain "validated with Infrared"; if any "validated" claim exists, change to
      "re-simulated with Infrared UTCI".
    Update coolspend/tests/test_surrogate.py if it asserts on the old Garcia-Nevado string; add an
    assertion that the surrogate provenance string mentions "Schrodi".
  </action>
  <verify>
    <automated>pytest coolspend/tests/test_surrogate.py coolspend/tests/test_optimizer.py -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Schrodi" coolspend/spatial_engine.py returns >= 1
    - grep -c "arXiv:2310.05691" coolspend/spatial_engine.py returns >= 1
    - grep -c "Rahman" coolspend/spatial_engine.py returns >= 1
    - grep -c "Schrodi" coolspend/optimizer.py returns >= 1
    - grep -ci "validated with infrared" coolspend/optimizer.py returns 0
    - grep -c "re-simulated with Infrared UTCI" coolspend/optimizer.py returns >= 1
    - No fabricated DOI: grep -c "doi.org" coolspend/spatial_engine.py returns 0 (PENDING tags only, no invented DOIs)
    - pytest coolspend/tests/test_surrogate.py coolspend/tests/test_optimizer.py exits 0
  </acceptance_criteria>
  <done>Surrogate citation re-anchored to Schrodi 2023 (arXiv, PENDING, magnitude-only) + Rahman 2022 with Garcia-Nevado demoted; artifact/audit JSON uses "re-simulated with Infrared UTCI" not "validated"; no fabricated DOIs; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code-emitted artifact JSON -> external readers (judges/auditors) | Overclaim strings ("validated", "88% vs naive") mislead readers about what is proven |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Repudiation (false claim) | artifact/audit JSON | mitigate | "validated with Infrared" → "re-simulated with Infrared UTCI"; "88% vs naive" computation + field deleted (D-12); citation re-anchored with PENDING tags (D-13) |
| T-05-12 | Tampering (fabricated provenance) | citation strings | mitigate | No fabricated DOIs; arXiv id + PENDING venue used verbatim; Schrodi cited for magnitude only, ML approach explicitly disclaimed |
</threat_model>

<verification>
- pytest coolspend/tests/test_optimizer.py + test_surrogate.py pass.
- grep confirms zero improvement_vs_naive / naive_baseline references in optimizer.py; Schrodi/Rahman present; no "validated with Infrared".
- Running save_outputs on mock backend produces an artifact lacking the deleted keys.
</verification>

<success_criteria>
The naive-baseline machinery and improvement_vs_naive_pct are gone; code-emitted artifact/audit
JSON uses honest "re-simulated with Infrared UTCI" phrasing; the surrogate ceiling cites
Schrodi 2023 + Rahman 2022 (Garcia-Nevado demoted) with no fabricated DOIs.
</success_criteria>

<output>
After completion, create `.planning/phases/05-surrogate-ground-truth-honesty-reset/05-04-SUMMARY.md`
</output>
