---
phase: 01-foundation-sdk-boundary
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - coolspend/spatial_engine.py
  - coolspend/data/angels_site.geojson
  - coolspend/tests/test_spatial_engine.py
  - coolspend/tests/test_coordinate_frame.py
  - MOCKS.md
autonomous: true
requirements: [SPATIAL-01, SPATIAL-02, SPATIAL-03]
must_haves:
  truths:
    - "spatial_engine loads a site GeoJSON (buildings, streets, site boundary) offline from a bundled fixture"
    - "is_valid_location(x_m, y_m) returns False for a point inside a building footprint"
    - "is_valid_location(x_m, y_m) returns False for a point on/near a street centerline"
    - "is_valid_location(x_m, y_m) returns False for a point outside the site boundary"
    - "is_valid_location(x_m, y_m) returns True for an open in-boundary point clear of buildings and streets"
    - "EPSG:4326 lat/lon converts to plaza-local metres at exactly one documented boundary function, and round-trips within tolerance"
  artifacts:
    - path: "coolspend/spatial_engine.py"
      provides: "GeoJSON site loader, is_valid_location, single 4326<->local-metres conversion"
      exports: ["load_site", "is_valid_location", "latlon_to_local_m", "local_m_to_latlon"]
      min_lines: 90
    - path: "coolspend/data/angels_site.geojson"
      provides: "offline mock site fixture: site boundary + >=1 building + >=1 street"
      contains: "FeatureCollection"
    - path: "coolspend/tests/test_spatial_engine.py"
      provides: "collision validity tests (building/street/outside/valid)"
      min_lines: 40
    - path: "coolspend/tests/test_coordinate_frame.py"
      provides: "single-boundary CRS conversion round-trip assertion"
      min_lines: 20
  key_links:
    - from: "coolspend/spatial_engine.py::is_valid_location"
      to: "loaded site geometry (buildings/streets/boundary)"
      via: "shapely contains / distance checks"
      pattern: "\\.contains\\(|\\.distance\\("
    - from: "coolspend/spatial_engine.py"
      to: "coolspend/data/angels_site.geojson"
      via: "load_site reads the bundled fixture relative to __file__"
      pattern: "Path\\(__file__\\)"
---

<objective>
Build `coolspend/spatial_engine.py`: load a site as OSM/GeoJSON (buildings, streets, site boundary) via shapely, expose `is_valid_location(x_m, y_m)` that rejects points inside buildings, on street centerlines, or outside the site boundary, and handle the EPSG:4326 to plaza-local-metres conversion at exactly ONE documented boundary. Ship a small offline GeoJSON fixture so all spatial tests run without network.

Purpose: This is the geometry gate the Phase 2 optimizer queries to reject invalid tree placements, and the single, tested coordinate frame that prevents the HIGH-severity coordinate footgun (CONCERNS.md 2.4) from silently producing spatially wrong Infrared payloads.
Output: `coolspend/spatial_engine.py`, `coolspend/data/angels_site.geojson`, `coolspend/tests/test_spatial_engine.py`, `coolspend/tests/test_coordinate_frame.py`, MOCKS.md fixture entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- Coordinate frame is the locked decision from ARCHITECTURE.md "Coordinate Systems".
     Plaza-local metres: SW corner of plaza rectangle = (0,0).
     x_m = East-West [0, 60], y_m = North-South [0, 42]. y=0 is the MACBA (north) edge.
     Plaza centroid in EPSG:4326 = (lon=2.1670, lat=41.3826). -->

Site constants to define at module top (from ARCHITECTURE.md + nature_nsga2_coolstock.py lines 74-86):
SITE_WIDTH_M = 60.0       # E-W extent
SITE_DEPTH_M = 42.0       # N-S usable extent
SITE_ORIGIN_LON = 2.1670  # EPSG:4326 lon of plaza centroid anchor
SITE_ORIGIN_LAT = 41.3826
HERITAGE_BUFFER_M = 5.0    # MACBA north-edge buffer

Projection (from ARCHITECTURE.md "Projection for small-area SDK calls"):
equirectangular with cos-latitude correction, accurate within +/-200 m of plaza centroid.
This conversion lives in EXACTLY ONE place: latlon_to_local_m() / local_m_to_latlon().
No other module may convert CRS (SPATIAL-03).

Public interface (from CONVENTIONS.md recommendation 11):
def load_site(path: str | None = None) -> dict          # parsed shapely geometries
def is_valid_location(x_m: float, y_m: float, site: dict | None = None) -> bool
def latlon_to_local_m(lon: float, lat: float) -> tuple[float, float]
def local_m_to_latlon(x_m: float, y_m: float) -> tuple[float, float]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the offline site GeoJSON fixture + MOCKS.md row</name>
  <read_first>
    - .planning/codebase/ARCHITECTURE.md (Coordinate Systems table — plaza-local frame, EPSG:4326 anchor)
    - .planning/codebase/CONVENTIONS.md (honesty contract Rule 1 — MOCKS.md row for fixture data)
  </read_first>
  <files>coolspend/data/angels_site.geojson</files>
  <action>
    Create `coolspend/data/angels_site.geojson` as a valid GeoJSON FeatureCollection in EPSG:4326 coordinates (lon, lat order per GeoJSON spec), anchored near the plaza centroid (lon 2.1670, lat 41.3826). Include at least three features distinguished by a `properties.kind` field:
      1. `kind="site_boundary"` — a Polygon roughly 60 m (E-W) by 42 m (N-S) around the centroid. Use small lon/lat offsets (about 0.0007 deg lon, about 0.00038 deg lat span at this latitude).
      2. `kind="building"` — at least one Polygon footprint INSIDE the boundary (e.g. the MACBA edge block along the north).
      3. `kind="street"` — at least one LineString centerline crossing the site.
    Keep it small and human-readable. This is a MOCK fixture site, not surveyed data. Add a MOCKS.md row: item `angels_site.geojson fixture`, location `coolspend/data/angels_site.geojson`, status `MOCK`, reason `hand-authored offline site geometry for deterministic spatial tests; not surveyed OSM data`, replacement path `real OSM export for Placa dels Angels`.
  </action>
  <verify>
    <automated>cd "C:/Users/Rafik/OneDrive/Python Resources/Hackathon" && python -c "import json; d=json.load(open('coolspend/data/angels_site.geojson')); kinds={f['properties']['kind'] for f in d['features']}; assert d['type']=='FeatureCollection'; assert {'site_boundary','building','street'} <= kinds, kinds; print('OK')" && grep -q "angels_site.geojson" MOCKS.md</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import json; json.load(open('coolspend/data/angels_site.geojson'))"` parses without error
    - The FeatureCollection contains features with `properties.kind` covering `site_boundary`, `building`, and `street`
    - `grep -q "angels_site.geojson" MOCKS.md` succeeds
  </acceptance_criteria>
  <done>A valid, small GeoJSON fixture with boundary + building + street exists offline; MOCKS.md documents it as a hand-authored mock.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement spatial_engine.py — loader, single CRS boundary, is_valid_location</name>
  <read_first>
    - coolspend/data/angels_site.geojson (the fixture this module loads; feature `kind` values)
    - .planning/codebase/ARCHITECTURE.md (Coordinate Systems section — the one-boundary rule and projection formula)
    - .planning/codebase/CONVENTIONS.md (no eval rule, module docstring, unit-suffix variable names, __main__ smoke block)
    - .planning/codebase/CONCERNS.md section 2.4 (coordinate footgun — HIGH severity)
  </read_first>
  <files>coolspend/spatial_engine.py</files>
  <behavior>
    - latlon_to_local_m(SITE_ORIGIN_LON, SITE_ORIGIN_LAT) returns a point at the plaza centroid in local metres; local_m_to_latlon of that result round-trips back to within 1e-6 deg
    - load_site() with no args loads the bundled coolspend/data/angels_site.geojson and returns a dict with shapely geometries keyed by kind (boundary Polygon, list of building Polygons, list of street LineStrings), all converted to local metres via the single boundary
    - is_valid_location(x_m, y_m) returns False when the point lies inside any building polygon
    - is_valid_location(x_m, y_m) returns False when the point lies within STREET_BUFFER_M of any street centerline
    - is_valid_location(x_m, y_m) returns False when the point lies outside the site boundary polygon
    - is_valid_location(x_m, y_m) returns True for an open, in-boundary point clear of buildings and streets
  </behavior>
  <action>
    Write `coolspend/spatial_engine.py` with `from __future__ import annotations` first, module docstring stating purpose + that geometry inputs are a MOCK fixture, and section separators per CONVENTIONS.md.

    Constants at top: the five constants from the interfaces block plus `STREET_BUFFER_M = 1.5` (rejection radius around street centerlines) and `DATA_DIR = Path(__file__).resolve().parent / "data"`, `DEFAULT_SITE = DATA_DIR / "angels_site.geojson"`.

    SINGLE CRS BOUNDARY (SPATIAL-03) — implement these two functions and DOCUMENT in their docstrings that they are the ONLY place CRS conversion happens in coolspend. Use equirectangular projection with cos-latitude correction (no external geodesy dep):
      `_M_PER_DEG_LAT = 111_320.0`
      `latlon_to_local_m(lon, lat)`: `x_m = (lon - SITE_ORIGIN_LON) * _M_PER_DEG_LAT * cos(radians(SITE_ORIGIN_LAT)) + SITE_WIDTH_M/2`; `y_m = (lat - SITE_ORIGIN_LAT) * _M_PER_DEG_LAT + SITE_DEPTH_M/2`. (The `+ SITE_*/2` offsets place the EPSG:4326 centroid anchor at the centre of the local-metre rectangle whose SW corner is (0,0).)
      `local_m_to_latlon(x_m, y_m)`: exact algebraic inverse of the above.

    `load_site(path=None)`: open `path or DEFAULT_SITE`, `json.load` it (NEVER eval — CONVENTIONS Rule 4). For each feature, read `properties.kind`. Convert every coordinate from (lon,lat) to local metres via `latlon_to_local_m`. Build shapely geometries: boundary -> `Polygon`, buildings -> `list[Polygon]`, streets -> `list[LineString]`. Import shapely at top: `from shapely.geometry import Polygon, LineString, Point`. Wrap the shapely import region defensively: if ImportError, raise a clear RuntimeError naming `shapely` (CONCERNS 5.3 — do not silently return 0.0). Return `{"boundary": Polygon, "buildings": [...], "streets": [...]}`. Cache the parsed default site in a module-level `_SITE_CACHE` dict so repeated calls do not re-read disk.

    `is_valid_location(x_m, y_m, site=None)`: `site = site or load_site()`. `p = Point(x_m, y_m)`. Return False if `not site["boundary"].contains(p)`; False if any `b.contains(p)` for b in buildings; False if any `s.distance(p) <= STREET_BUFFER_M` for s in streets; else True.

    Add an `if __name__ == "__main__":` smoke block printing is_valid_location for one valid and one in-building point.
  </action>
  <verify>
    <automated>cd "C:/Users/Rafik/OneDrive/Python Resources/Hackathon" && python -c "from coolspend.spatial_engine import load_site, is_valid_location, latlon_to_local_m, local_m_to_latlon, SITE_ORIGIN_LON, SITE_ORIGIN_LAT; xy=latlon_to_local_m(SITE_ORIGIN_LON,SITE_ORIGIN_LAT); ll=local_m_to_latlon(*xy); assert abs(ll[0]-SITE_ORIGIN_LON)<1e-6 and abs(ll[1]-SITE_ORIGIN_LAT)<1e-6; s=load_site(); assert s['boundary'] is not None and len(s['buildings'])>=1 and len(s['streets'])>=1; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eq "def latlon_to_local_m|def local_m_to_latlon" coolspend/spatial_engine.py` succeeds (single conversion boundary present)
    - `grep -q "def is_valid_location" coolspend/spatial_engine.py` succeeds
    - `grep -Eq "\.contains\(|\.distance\(" coolspend/spatial_engine.py` succeeds (shapely collision used)
    - `grep -q "eval(" coolspend/spatial_engine.py` returns NO match (exit 1)
    - The verify command prints `OK`
  </acceptance_criteria>
  <done>spatial_engine loads the fixture into local-metre shapely geometries via a single documented CRS boundary; is_valid_location enforces boundary/building/street rules; CRS round-trips within tolerance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests — collision validity (SPATIAL-01/02) and single-boundary CRS (SPATIAL-03)</name>
  <read_first>
    - coolspend/spatial_engine.py (public API + constants under test)
    - coolspend/data/angels_site.geojson (to pick concrete in-building / on-street / outside / open coordinates)
    - .planning/codebase/CONCERNS.md section 4.1 (test_coordinate_frame.py is explicitly recommended)
  </read_first>
  <files>coolspend/tests/test_spatial_engine.py, coolspend/tests/test_coordinate_frame.py</files>
  <behavior>
    - test_spatial_engine.py::test_inside_building_rejected: a point inside a building footprint -> is_valid_location is False
    - test_spatial_engine.py::test_on_street_rejected: a point within STREET_BUFFER_M of a street centerline -> False
    - test_spatial_engine.py::test_outside_boundary_rejected: a point outside the site boundary (e.g. x_m=-5) -> False
    - test_spatial_engine.py::test_open_location_valid: an open in-boundary point clear of buildings/streets -> True
    - test_coordinate_frame.py::test_roundtrip: latlon_to_local_m then local_m_to_latlon recovers the original lon/lat within 1e-6 deg for the plaza centroid and one offset point
    - test_coordinate_frame.py::test_origin_maps_into_site: the EPSG:4326 site origin maps to a local-metre point inside [0,SITE_WIDTH_M] x [0,SITE_DEPTH_M]
  </behavior>
  <action>
    Write the two test files using pytest. In `test_spatial_engine.py`, load the site once via a fixture; derive concrete test coordinates by computing the centroid of the first building polygon (in-building point), a point offset 0.5 m perpendicular from a street vertex (on-street point), a clearly outside point, and an open interior point (e.g. boundary centroid nudged away from buildings/streets — if the centroid is occupied, scan a small grid for the first valid open point and assert at least one exists). Implement the four named tests. In `test_coordinate_frame.py`, implement the two named round-trip / containment tests directly against the conversion functions. All tests run offline against the bundled fixture.
  </action>
  <verify>
    <automated>cd "C:/Users/Rafik/OneDrive/Python Resources/Hackathon" && python -m pytest coolspend/tests/test_spatial_engine.py coolspend/tests/test_coordinate_frame.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest coolspend/tests/test_spatial_engine.py coolspend/tests/test_coordinate_frame.py -q` exits 0
    - `grep -q "is_valid_location" coolspend/tests/test_spatial_engine.py` succeeds
    - `grep -Eq "latlon_to_local_m|local_m_to_latlon" coolspend/tests/test_coordinate_frame.py` succeeds
  </acceptance_criteria>
  <done>Collision tests prove building/street/outside rejection and open-point acceptance; coordinate-frame test asserts the single CRS boundary round-trips within tolerance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → process | angels_site.geojson is parsed at load time; treated as semi-trusted local input |
| local-metre frame → Infrared payload (Phase 2+) | a CRS error here yields plausible but spatially wrong geometry sent to the API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering / code injection | GeoJSON parsing | mitigate | Parse with `json.load` only; never `eval`/`exec` on file contents (CONVENTIONS Rule 4); shapely consumes coordinate arrays, not code |
| T-01-06 | Integrity (spatial correctness) | CRS conversion | mitigate | All EPSG:4326<->local-metre conversion confined to one documented boundary (SPATIAL-03); round-trip asserted by test_coordinate_frame.py |
| T-01-07 | Spoofing | mock fixture mistaken for real survey | mitigate | MOCKS.md row marks angels_site.geojson as hand-authored MOCK with replacement path |
| T-01-08 | DoS | malformed/huge GeoJSON | accept | Local fixture only; not user-uploaded in this phase; size bounded by hand-authored file |
</threat_model>

<verification>
- `python -c "from coolspend import spatial_engine"` succeeds.
- `python -m pytest coolspend/tests/test_spatial_engine.py coolspend/tests/test_coordinate_frame.py -q` exits 0.
- Exactly one pair of CRS-conversion functions exists; no other module converts CRS.
- No `eval`/`exec` in spatial_engine.py.
- MOCKS.md documents the fixture site.
</verification>

<success_criteria>
- SPATIAL-01: site loads from OSM/GeoJSON (buildings, streets, boundary) offline.
- SPATIAL-02: is_valid_location rejects in-building, on-street-centerline, and outside-boundary points; accepts open points — proven by passing tests.
- SPATIAL-03: EPSG:4326 to plaza-local-metres conversion implemented at exactly one documented boundary and asserted by test_coordinate_frame.py.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-sdk-boundary/01-02-SUMMARY.md`
</output>
