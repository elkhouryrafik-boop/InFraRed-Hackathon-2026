---
phase: 01-foundation-sdk-boundary
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - coolspend/cost_model.py
  - coolspend/tests/test_cost_model.py
  - MOCKS.md
autonomous: true
requirements: [COST-01, COST-02]
must_haves:
  truths:
    - "Per-tree cost is computed as CapEx (planting) + OpEx (maintenance) from documented module-level constants"
    - "The CapEx/OpEx assumption constants are visible as named constants in cost_model.py"
    - "cost_per_utci_degree(config) returns a finite euro-per-degC value given a config with a tree count and a validated UTCI delta"
    - "A zero or negative UTCI delta does not crash cost_per_utci_degree (guarded), and is flagged"
  artifacts:
    - path: "coolspend/cost_model.py"
      provides: "CapEx/OpEx constants, per_tree_cost, total_cost, cost_per_utci_degree"
      exports: ["per_tree_cost", "total_cost", "cost_per_utci_degree"]
      min_lines: 70
    - path: "coolspend/tests/test_cost_model.py"
      provides: "deterministic cost-model math tests"
      min_lines: 30
  key_links:
    - from: "coolspend/cost_model.py::cost_per_utci_degree"
      to: "total_cost + validated UTCI delta"
      via: "euro / degC division with zero-delta guard"
      pattern: "def cost_per_utci_degree"
    - from: "coolspend/cost_model.py::per_tree_cost"
      to: "CapEx + OpEx constants"
      via: "CAPEX_PER_TREE_EUR + OPEX_PER_TREE_YEAR_EUR * horizon"
      pattern: "CAPEX_PER_TREE_EUR|OPEX_PER_TREE"
---

<objective>
Build `coolspend/cost_model.py`: per-tree cost = CapEx (planting) + OpEx (maintenance) with documented constants and assumptions (COST-01), and `cost_per_utci_degree(config)` returning the headline euro-per-degC KPI for a configuration (COST-02). Pure arithmetic, no external API, fully offline.

Purpose: This is the headline decision metric ("most degrees of comfort per euro") the whole project optimizes for. Phase 2's ranking and Phase 3's display cards read this KPI directly. Every cost assumption must be a visible, documented constant — no buried magic numbers — and any assumed value gets a MOCKS.md row per the honesty contract.
Output: `coolspend/cost_model.py`, `coolspend/tests/test_cost_model.py`, MOCKS.md assumption entry.

Wave note: this plan runs after 01-02 because both append to the shared `MOCKS.md` ledger; serialising avoids a concurrent-write conflict on that file. The module itself depends only on the package created in 01-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- This is a NEW composition (no verbatim port). The reference cost lived in
     scaffold modules (nature_nsga2_coolstock.py); coolspend is TREES, so the
     scaffold cost is dropped and replaced with per-tree CapEx+OpEx. -->

Config dict shape (subset of the locked output JSON schema in ARCHITECTURE.md):
A configuration is a dict that includes at least:
  - a tree count: use key "tree_count" (int). Require "tree_count"; do NOT reuse the
    scaffold "scaffold_modules" field (that was the reference scaffold problem, not trees).
  - a UTCI delta in degC: use key "delta_utci_c" (validated real delta) and fall back
    to "delta_tmrt_c" (surrogate) only if delta_utci_c absent, with a flag in the result.

Standard metric return shape (CONVENTIONS.md "Return Shape Convention"):
public compute functions return a dict with keys:
  value, unit, confidence ("HIGH"|"MED"|"LOW"), sources (list), note (str), metric_id (str)
Confidence constants are module-level strings: HIGH="HIGH"; MED="MED"; LOW="LOW".

Public interface:
def per_tree_cost(horizon_years: int = OPEX_HORIZON_YEARS) -> float           # euros
def total_cost(config: dict) -> float                                         # euros
def cost_per_utci_degree(config: dict) -> dict                                # standard metric dict, value in EUR/degC
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement cost_model.py — documented CapEx/OpEx constants and the euro-per-degC KPI</name>
  <read_first>
    - .planning/codebase/CONVENTIONS.md (return-shape dict convention, confidence constants, unit-suffix names, honesty Rule 1 + Rule 6 framing, __main__ smoke block)
    - .planning/codebase/ARCHITECTURE.md (Cost Model layer + output JSON field names like delta_tmrt_c)
    - nature_metrics.py lines 1-60 (the standard metric-dict return style to mirror — read only the header/early functions; do NOT port the EPW/GBIF ingest)
  </read_first>
  <files>coolspend/cost_model.py</files>
  <behavior>
    - per_tree_cost() returns CAPEX_PER_TREE_EUR + OPEX_PER_TREE_YEAR_EUR * OPEX_HORIZON_YEARS as a finite positive float
    - per_tree_cost(horizon_years=0) returns exactly CAPEX_PER_TREE_EUR (no OpEx)
    - total_cost({"tree_count": 10}) == 10 * per_tree_cost()
    - cost_per_utci_degree({"tree_count": 10, "delta_utci_c": 0.5}) returns a dict with finite value == total_cost / 0.5, unit "EUR/degC", confidence "MED" or "HIGH", metric_id "cost_per_utci_degree"
    - cost_per_utci_degree with delta_utci_c absent but delta_tmrt_c present uses the surrogate delta and sets confidence "LOW" with a note flagging surrogate use
    - cost_per_utci_degree with delta <= 0 returns value None WITHOUT raising, confidence "LOW", note explaining non-positive delta
  </behavior>
  <action>
    Write `coolspend/cost_model.py` with `from __future__ import annotations` first, a module docstring (purpose + that constants are DECLARED assumptions pending sourcing), section separators, and confidence constants `HIGH/MED/LOW`.

    Documented CapEx/OpEx constants (COST-01) — define as module-level UPPER_SNAKE_CASE with inline source/assumption comments (these are DECLARED assumptions, NOT verified — flag in MOCKS.md):
      CAPEX_PER_TREE_EUR = 350.0     # planting: nursery stock + labour + initial irrigation (DECLARED assumption, urban street-tree midrange)
      OPEX_PER_TREE_YEAR_EUR = 35.0  # annual maintenance: watering, pruning, inspection (DECLARED assumption)
      OPEX_HORIZON_YEARS = 10        # amortisation horizon for OpEx in the headline KPI (DECLARED assumption)
    Each constant MUST carry the unit suffix (`_EUR`, `_YEAR_EUR`, `_YEARS`) per CONVENTIONS naming. Add a `# SOURCE: REQUIRES_VERIFICATION` note — do NOT invent a citation (honesty Rule 2).

    `per_tree_cost(horizon_years: int = OPEX_HORIZON_YEARS) -> float`: return `CAPEX_PER_TREE_EUR + OPEX_PER_TREE_YEAR_EUR * horizon_years`.

    `total_cost(config: dict) -> float`: read `tree_count = int(config.get("tree_count", 0))`; return `tree_count * per_tree_cost()`. If tree_count <= 0 return 0.0.

    `cost_per_utci_degree(config: dict) -> dict` (COST-02): determine the delta — prefer `config.get("delta_utci_c")`; if None, use `config.get("delta_tmrt_c")` and mark confidence LOW + note "surrogate delta used (delta_utci_c absent) — replace with validated UTCI". Compute `cost = total_cost(config)`. Guard: if delta is None or `delta <= 0`, return the standard dict with `value=None`, confidence LOW, note explaining the non-positive/missing delta (do NOT divide — Rule 6 honest framing, no fabricated KPI). Otherwise `value = round(cost / delta, 2)`, confidence MED (HIGH only if delta came from delta_utci_c). Always return the standard metric dict: `{value, unit:"EUR/degC", confidence, sources:["DECLARED: CapEx/OpEx assumptions (REQUIRES_VERIFICATION)"], note, metric_id:"cost_per_utci_degree"}`.

    Add an `if __name__ == "__main__":` smoke block printing per_tree_cost() and cost_per_utci_degree for a sample config `{"tree_count": 20, "delta_utci_c": 0.42}`.

    Then add a MOCKS.md row: item `CapEx/OpEx tree cost constants`, location `coolspend/cost_model.py`, status `DECLARED`, reason `CAPEX_PER_TREE_EUR/OPEX_PER_TREE_YEAR_EUR/OPEX_HORIZON_YEARS are unsourced midrange assumptions`, replacement path `verified municipal tree-cost figures`.
  </action>
  <verify>
    <automated>cd "C:/Users/Rafik/OneDrive/Python Resources/Hackathon" && python -c "from coolspend.cost_model import per_tree_cost, total_cost, cost_per_utci_degree, CAPEX_PER_TREE_EUR; assert per_tree_cost(0)==CAPEX_PER_TREE_EUR; assert total_cost({'tree_count':10})==10*per_tree_cost(); r=cost_per_utci_degree({'tree_count':10,'delta_utci_c':0.5}); assert r['value']==round(10*per_tree_cost()/0.5,2) and r['unit']=='EUR/degC'; z=cost_per_utci_degree({'tree_count':10,'delta_utci_c':0.0}); assert z['value'] is None; print('OK')" && grep -q "CapEx/OpEx" MOCKS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eq "CAPEX_PER_TREE_EUR|OPEX_PER_TREE_YEAR_EUR|OPEX_HORIZON_YEARS" coolspend/cost_model.py` succeeds (constants visible)
    - `grep -q "def cost_per_utci_degree" coolspend/cost_model.py` succeeds
    - `grep -q "REQUIRES_VERIFICATION" coolspend/cost_model.py` succeeds (no fabricated citation)
    - The verify command prints `OK`
    - `grep -q "CapEx/OpEx" MOCKS.md` succeeds
  </acceptance_criteria>
  <done>cost_model.py exposes documented CapEx/OpEx constants, per_tree_cost, total_cost, and cost_per_utci_degree returning a finite EUR/degC KPI with a zero-delta guard; assumptions logged in MOCKS.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests — cost-model math and KPI edge cases (COST-01/02)</name>
  <read_first>
    - coolspend/cost_model.py (functions, constants, return-dict shape under test)
    - .planning/codebase/CONVENTIONS.md (standard metric dict keys to assert on)
  </read_first>
  <files>coolspend/tests/test_cost_model.py</files>
  <behavior>
    - test_per_tree_cost_capex_only: per_tree_cost(0) == CAPEX_PER_TREE_EUR
    - test_per_tree_cost_includes_opex: per_tree_cost() > CAPEX_PER_TREE_EUR (OpEx adds over the horizon)
    - test_total_cost_scales: total_cost({"tree_count": n}) == n * per_tree_cost() for n in {0, 1, 25}
    - test_cost_per_degree_value: cost_per_utci_degree with delta_utci_c returns value == total_cost/delta, unit "EUR/degC", confidence in {"MED","HIGH"}
    - test_surrogate_fallback_flagged: config with only delta_tmrt_c yields confidence "LOW" and a note mentioning surrogate
    - test_nonpositive_delta_guarded: delta_utci_c == 0 (and a negative case) -> value is None, no exception raised, confidence "LOW"
    - test_returns_standard_metric_dict: result has keys value, unit, confidence, sources, note, metric_id
  </behavior>
  <action>
    Write `coolspend/tests/test_cost_model.py` using pytest. Import the public API and `CAPEX_PER_TREE_EUR` from `coolspend.cost_model`. Implement the seven named tests asserting the math relationships and the standard-dict shape. Tests are pure arithmetic, fully offline and deterministic.
  </action>
  <verify>
    <automated>cd "C:/Users/Rafik/OneDrive/Python Resources/Hackathon" && python -m pytest coolspend/tests/test_cost_model.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest coolspend/tests/test_cost_model.py -q` exits 0
    - `grep -q "cost_per_utci_degree" coolspend/tests/test_cost_model.py` succeeds
    - `grep -Eq "metric_id|unit" coolspend/tests/test_cost_model.py` succeeds (asserts standard dict shape)
  </acceptance_criteria>
  <done>All cost-model tests pass offline, proving CapEx+OpEx composition, the EUR/degC KPI, surrogate-fallback flagging, and the non-positive-delta guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config dict → cost computation | config originates from the optimizer/app; numeric fields drive arithmetic only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Repudiation / Integrity | unsourced cost constants presented as fact | mitigate | Constants are visible, commented as DECLARED assumptions, tagged REQUIRES_VERIFICATION, and logged in MOCKS.md (honesty Rules 1/2/6) |
| T-01-10 | Tampering | divide-by-zero / NaN KPI from bad delta | mitigate | Non-positive or missing delta returns value=None with LOW confidence rather than raising or fabricating a number |
| T-01-11 | Information Disclosure | n/a (pure arithmetic, no secrets/IO) | accept | Module performs no IO, network, or secret handling |
</threat_model>

<verification>
- `python -c "from coolspend import cost_model"` succeeds.
- `python -m pytest coolspend/tests/test_cost_model.py -q` exits 0.
- CapEx/OpEx constants are visible named constants; no fabricated citation (REQUIRES_VERIFICATION used).
- MOCKS.md documents the cost assumptions as DECLARED.
</verification>

<success_criteria>
- COST-01: per-tree cost = CapEx + OpEx with documented assumptions visible as constants in cost_model.py.
- COST-02: cost_per_utci_degree(config) returns a finite EUR/degC KPI (with a guarded non-positive-delta path).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-sdk-boundary/01-03-SUMMARY.md`
</output>
